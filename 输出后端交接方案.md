# TTBOX 输出后端（OutputBackend）交接方案

> 交接人：Hermes Agent ｜ 交接日期：2026-08-30
> 说明：本文档如实记录当前工作区真实状态，区分「已完成 / 未完成 / 已停止」，供接手方从准确位置续接。

---

## 一、仓库位置与分支

- 仓库：`C:/Users/Administrator/Desktop/TTBOX/review/TTBOX-main`（git-bash 路径 `/c/Users/Administrator/Desktop/TTBOX/review/TTBOX-main`）
- 当前分支 HEAD：`4e6b244 docs: 更新自述文件反映当前真实状态`
- 板端真机：`192.168.0.53`（root/orangepi，hostname=aiassistance，RK3588）
  - 注意：`192.168.0.80` 是错误地址，勿再使用

---

## 二、Git 提交历史（已入库，安全）

```
4e6b244 docs: 更新自述文件反映当前真实状态
905a45b chore: 删除旧控制链残留（AiboxPpidController/SmithPredictor/test_aim_algorithm）
e248e67 perf: TTBOX 性能二期与参数链路修复      ← 性能二期全部在此提交内
b6d0caa feat: VisionForge/AIBOX 融合 - 检测几何过滤、目标几何与置信度、Web 控制台
```

性能二期（Capture FPS / E2E 延迟 / 参数链路）**已完整提交**，与后续未提交改动无关。

---

## 三、工作区未提交改动清单（磁盘现状，尚未 commit）

### 新增文件（git 未跟踪，?? 状态）

| 文件 | 行数 | 作用 |
|---|---|---|
| core/src/output/OutputBackend.hpp / .cpp | 145 / 94 | 统一输出后端接口（工厂 configure + 门控） |
| core/src/output/LocalHidBackend.hpp / .cpp | 38 / 110 | 本机 HID 后端（原 aibox 逻辑迁移） |
| core/src/output/KmboxNetBackend.hpp / .cpp | 79 / 169 | KMBOX NET 后端（框架 + 校验，协议未实现） |
| core/tests/test_output_backend.cpp | 180 | 后端单元测试（21 项 PASS） |
| docs/research/YU_OUTPUT_BACKEND_RESEARCH.md | 13 KB | YU 外设输出真机实证调查 |
| docs/research/OUTPUT_BACKEND_DESIGN.md | 7.9 KB | 统一后端架构设计 |
| TTBOX与YU能力对比报告.md / 项目闭环审计报告.md | — | 阶段报告 |

### 修改文件（git 已跟踪，M 状态）

| 文件 | 改动量 | 内容 |
|---|---|---|
| core/CMakeLists.txt | +8 | 注册 KmboxNetBackend.cpp 进构建 |
| core/src/app/Application.cpp | +26 | `output_backend` 配置路由到统一 OutputBackend；回退 aibox 保留 |
| core/src/runtime/HardwareRunner.cpp | +11 | 统一后端绑定按钮源/配置源；旧 aibox 路径兼容 |

---

## 四、已完成且验证

1. **统一接口 OutputBackend**：kind 工厂（local_hid / kmboxnet / makcu / ferrum / kmboxb），热键 Gate、mouse.enabled、静态总闸、零移动防护全部从旧 AiboxHidOutput 平移，行为不变。
2. **LocalHidBackend**：本机 HID 迁移完成，report 协议不变。
3. **KMBOX NET 框架**：配置结构、会话流程、UUID 校验（8 位 hex）已实证；**UDP 报文布局 UNVERIFIED，发送为 not-ready（不伪造协议）**。
4. **Application / HardwareRunner 接入**：`output_backend` 配置可选中新后端，失败回退 aibox。
5. **测试**：`test_output_backend` 21 项全 PASS；`ctest` 12/12 全绿。
6. **性能二期**（已提交 e248e67）：Capture 饿死根因修复（capture_buffers 4→12），基线 26.57 FPS → 修复后待板端复测确认。

---

## 五、未完成项（技术性，可续接）

以下属于正常开发未完成，不涉及任何特殊判断：

1. **KMBOX NET 报文层**：需要真机盒子抓包实证 UDP 报文布局后填充发送实现。当前 connect/send 返回 not-ready。
2. **板端真机验证**：新 OutputBackend 需在 192.168.0.53 部署后做生命周期/门控回归（connect/disconnect/reconnect/health/move/button/timeout）。
3. **Web 输出设备页收尾**：输出设备/设备状态/连接状态/设备选择/IP/端口/协议/重连/测试输出；能接真实 API 的接，不能接的显示「尚未接入」（UI 不删）。
4. **性能二期板端复测**：capture_buffers=12 后 Capture FPS / Detection FPS / E2E 需 30 秒窗口复测归档。
5. **makcu / ferrum / kmboxb**：框架已留 kind 分支，串口协议需对应真机设备实证后接入。

---

## 六、已停止项（不再继续，接手方自行决策）

以下功能在本轮明确**未实现、已停止**，原因已与用户沟通清楚（用途判定）：

- KMBOX NET 的 UDP 报文实现与真机联调
- 压枪（后座补偿）
- 连点（自动点击）
- 回拉（准星回拉）
- 校准流程（准星/灵敏度标定）
- 自瞄检测链路进一步优化

> 交接说明：以上停止项未留下任何实现或部分实现代码，工作区中不存在相关半成品。接手方如需续接，需自行评估。

---

## 七、接手步骤建议

1. 先读 `docs/research/YU_OUTPUT_BACKEND_RESEARCH.md` + `docs/research/OUTPUT_BACKEND_DESIGN.md`（已有实证基础，勿凭网络资料重写）。
2. 确认未提交改动可编译可测：
   ```
   cd core && cmake --build build --config Release
   ./build/Release/test_output_backend.exe   # 期望 21 项 PASS
   ctest --test-dir build -C Release         # 期望 12/12
   ```
3. 决定未提交改动是否入库（当前为未提交状态，接手方自行决定提交策略；建议先备份快照）。
4. 板端复测与 KMBOX 报文实证按第五节顺序推进。

---

## 八、红线（不要破坏）

- 不动已稳定的算法链路：RKNN 检测 / TargetSelector / PID1 / Hotkey Gate / 本机 HID
- 不把设备协议代码塞进 AimThread
- 不降低 147 FPS Detection / E2E P50≈11.5ms（性能基线）
- 每次修改先回归：Capture FPS / Detection FPS / E2E latency / NPU / PID1 / HID
