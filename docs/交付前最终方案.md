# TTBOX 交付前最终方案

> 建立 2026-09-18。**这是执行用的活真源**：开工后一切按本文件走，改一步更新一步。
> 逐条编号与定案 → `docs/交付前逐项定案台账-2026-09-18.md`（控件级 / 路由级 / 文件级）
> 事实取证 → `docs/交付前确定性盘点-目录-假功能-重复项-2026-09-18.md`、
> `docs/路由真值表-2026-09-18.md`、`docs/漏搬清单-2026-09-18.md`
>
> **状态：方案已定，不再有待你拍板的事项。**
> 只剩两条"等取证"的项（无线能否救活、6 个 core 头文件死没死），都有默认动作，**不阻塞开工**，见 §六。
> 代码仍在冻结 —— 等你说一句"可以开始了"，从 §三 的 S1 起步。

---

## 一、一句话

把面板漏搬的补回来、把"能填能存不生效"的配置真正接到后端、把平台错配的假功能删干净、
把更新 / 主题 / 远端这类半成品做成真的；**分两批做**，每批都走
「Windows 本地测通 → WSL 交叉编译 → RK3588 实机实测」。

## 二、三条硬约束（沿用你定死的流程，不变）

1. **Windows 是源码唯一真源**，板端只跑二进制、不改码。
2. **本地没 PASS 不上板**；没有真实测试证据不写 PASS。
3. **拿到"可以开始了"再动手** —— 现在仍在冻结期。

---

## 三、执行顺序

### 第一批：离线闭环（不依赖云端，做完即可出包上板）

| 步 | 做什么 | 主要落点 | 拿什么证明 |
|----|--------|----------|-----------|
| **S1 减法清场** | 先做不需要动脑子的减法，把后面要碰的面缩小 | 见下方 S1 明细 | 面板能开、页签 12→9、`grep` 证明端点已卸载、`ctest` 全绿 |
| **S2 配置真接** | 约 24 项"只存不生效"的字段在后端补映射，并让丢弃字段回显 | `plugins/web/bin/ttbox-web.py:766-992` C `web_body_to_profile()` / `profile_to_web()` | 逐字段"改→存→刷新读回"值一致 |
| **S3 面板补搬** | 5 个漏搬子页签 + 5 个漏搬控件 + 3 个前端小缺陷 | `plugins/web/templates/index.html`；可复用 `templates/motion_training.html` | 浏览器逐页点，改一项存一项读一项 |
| **S4 core 侧真控** | 风扇真控、辅助功能 4 块落链、两处叠加层绘制、存储扩容真流程 | `core/src/`（`Application.cpp:344-379`、风扇硬编码满转处） | `ctest` 全绿 + 板端实测 |
| **S5 数据归一 + 清理** | 云会话/模型库/主配置归一、乱码根治、板端清场 | 后端 + 板端 | 重启后 5 个 unit 全 active、模型名正常 |
| **S6 出包上板** | 本地测试 → 交叉编译 → 装机 → 真机实测 | `scripts/ttbox_release_install.sh` | 八段验收链全过 |

#### S1 明细（照单执行）

- **面板移页**：07 Hailo-8 加速 / 08 键鼠盒子 / 09 无线 —— 三页整页移除（`plugins/web/templates/index.html`）。
- **端点删除**（都在 `plugins/web/bin/ttbox-web.py`）：
  `/api/hailo/status`、`/api/hailo/install`、`/api/mouse-output/test-circle`、
  `/api/makcu|ferrum|kmboxb/devices`、`/api/system/master-reactivate`、`/api/system/version`、
  `/api/health/frontend`、`/api/settings/auto-start`、`/api/hardware/mouse/timing`、
  `/api/models/game-profile`、`/api/models/cloud-encrypted`、`/api/preview.jpg`、
  `/api/activation/network/prepare`；`/api/ota/install` 去重（只留一份实现）。
- **core 删功能**：`Application.cpp:344-379` 的 4 个盒子 kind 分支、`OutputBackend.cpp:102-109` 的
  kind 判定、`core/src/output/MakcuMouseBackend.cpp`（真实现但从未被 include）、配置 schema 的 kind 枚举。
  ⚠️ 注意 `output_backend` 默认值 `"aibox"` 本身就落在兜底分支上 ⇒ "未知 kind 一律 fail-closed"**不能一刀切**，
  只能对"曾存在、现已删除"的 kind 显式告警。
- **整文件移出出货包**：`plugins/web/api_v1.py`（47 路由）、`plugins/web/framework_api.py`（26 路由）、
  `plugins/web/static/legacy/`（640KB）、`platform/`（25 文件）、`scripts/wifi_manager.py`。
- **仓库清理**：根 `third_party/`（2 文件，与 `core/third_party/` 逐字节相同）、
  根 `_probe_bash.txt`/`_probe_ps.txt`/`_probe_ssh.txt`、`out/`（15M）、根 7 个 release 旧副本、
  11 个非 release 目录（**保留 `config`/`models`**，那是运行时数据）。
- **同步** `RELEASE_MANIFEST.json`；顺手改掉 `ui-custom.css` 那句过时注释。
- **测试侧**：`framework/tests/test_web_plugin.py` 的失效断言改掉；`ttbox_fhs_init.sh` 与 payload 的
  `tests/` 排除口径统一。
- 删已跟踪文件一律 **`rm -f` + `git add -A -- <path>`**，本工作区**禁 `git rm`**（会把整棵树删掉）。

#### S2 明细

- 补映射：热键 9 项（`hotkey_guard_*`、`zoom_*`、`toggle_offset_*`）、移动控制 8 项、
  辅助功能 4 整块（自动开火 / 连点 / 自动背闪 / 准星找色）、模型库 2 项、风扇 3 项、USB 描述符 11 字段。
- `profile_to_web()` 去掉硬编码占位值（现在这是"界面弹回默认"的直接原因）。
- 新增 `ignored_fields` 回显：未识别字段列在响应里，**把"静默丢弃"这个行为本身改掉**，
  否则以后每加一个控件都会重犯同一个坑。
- 多热键：`ttbox-web.py:905-907` 现在只取 `aim_profiles[0]`，改成遍历全部。
- 转火延迟：改名回后端认的 `controller_selector_lost_grace_ms`，单位由"帧数"改成"毫秒"。

#### S3 明细

- 补 5 个子页签：自动标定、拟人化整形（`personal_motion`）、锁定确认（`lock_confirm`）、
  头部瞄准（`head_aim`）、个性曲线训练。后端映射（23/21/19 处）与 core 实现都在，缺的只是 UI。
- 补 5 个控件：屏蔽物理按键整块弹窗、USB 鼠标等待时间（`mouse_settle_delay_sec`，
  后端**强校验必填**，现在新 UI 没法设）、预设重命名（后端 `action=='rename'` 真实现）、
  环出检测框叠加面板、首页目标框叠加层。
- 模型导入的 `preset_file` 入口。
- 三个前端小缺陷：`releaseGuardSensitivity` 残留 JS 删掉；标定结束改成**校验后端返回**再弹"已应用"；
  免责声明未勾选时禁用保存按钮。

---

### 第二批：云端相关（要等两个前置）

**前置**：① 正式服务器地址（现在只有 Cloudflare 临时隧道；第 6 轮定的是"先做可配化"，
地址留空 + 启动 fail-closed）；② 转换器能否随包（会改 payload 组成与 `RELEASE_MANIFEST.json`）。

| 步 | 做什么 | 卡在哪 |
|----|--------|--------|
| **S7** | 更新发现机制：`/api/update/versions`、`/check`、`/cleanup-stuck` 真实现（`install`/`status` 本来就是真的） | 要云端 update-server |
| **S8** | 主题商店（6 条子关系 + 恢复独立页）、品牌背景（4 条）、远端连接（4 条） | 远端要定协议 |
| **S9** | 激活：本地全量恢复真实现 | 无外部依赖，只是量大 |
| **S10** | 模型导入真通：ONNX / HEF + 转换器随包 + NPU 并发接上 | 转换器要不要打进包 |

---

## 四、为什么分两批

第一批每一条都能在**离线环境自证**——不依赖任何外部服务，出一次包就能上板验完，
风险全在自己手里。第二批每一条都要外部输入（服务器地址、协议、转换器），
混在第一批里只会让第一批也被堵住。

## 五、我替你定的 20 条默认值（**不同意就直接点哪一条改**）

| 编号 | 条目 | 我定的 |
|------|------|--------|
| A502 | 模型库 `remote-frame-format` | 真接上（core 读该字段） |
| A503 | 参数旁"提高此值可提高检测FPS"文案 | 改成与真实行为一致的文案 |
| A504 | 校准图上传 `modelCalibrationZip` | 真接上：后端读 `calibration_zip` 并落盘 |
| A509 | 模型导入 `preset_file` | 补 UI 入口 |
| — | 第 11 轮"模型类六条"的准确范围 | 按**两组都要**：后端 6 条接口（`rknn-concurrency`、`device-code`、`game-profile`、`cloud-encrypted`、`/api/model/list`、`/api/model/active`）+ 前端 6 项（帧格式、校准图文案、校准图上传、ONNX、HEF、并发深度） |
| — | `/api/system/version` | 删（`/api/state` 已经给 `app_version`） |
| — | `/api/health/frontend` | 删（没有任何前端在上报） |
| — | `/api/settings/auto-start` | 删（core 没有对应读取点） |
| — | `/api/hardware/mouse/timing` | 删（与已生效的 A605 时序组重复） |
| — | `/api/models/game-profile` | 删（无 UI、无 core 消费） |
| — | `/api/models/cloud-encrypted` | 删（云端无此服务，恒失败） |
| — | `/api/preview.jpg` | 删（前端用的是 `/api/preview.mjpg`） |
| — | `/api/activation/network/prepare` | 删（恒 `attempted=True, changed=False`） |
| E01 | `test_web_plugin.py` 失效断言 | 现在就修 |
| E02 | `tests/` 排除口径冲突 | 现在就修 |
| E09 | 安装脚本权限 / 镜像产物 | 随镜像阶段 |
| A1106 | 设备码 | 保持只读 |
| A0-3f | `ui-custom.css` 过时注释 | 改注释 |
| C04 | `static/legacy/` + `platform/` + `api_v1.py` | 全部移出出货包 |
| — | 分批方式 | 分两批（§三） |

## 六、只剩两条"等取证"（有默认动作，**不阻塞开工**）

| 编号 | 等什么 | 谁在查 | 默认动作 |
|------|--------|--------|---------|
| A904 | 板端无线能不能救活（DTB / 固件 / 驱动） | Explore-1 正在板端取证 | 救不活 ⇒ 按已定执行（移除无线页签、删 `wifi_manager.py`）；救得活 ⇒ 单开一项恢复 |
| E08 / C07 | `core/src/mouse/` 那 6 个头文件死没死 | Explore-2 正在取证 | 真死 ⇒ 删；仅被单测引用 ⇒ **保留 + 注释标注"仅测试用"**，不误伤 `core/tests/test_mouse.cpp` |
| C10 | 两个空 `presets/` 哪个是活的 | Explore-2 正在取证 | 保留被代码引用的那个，删另一个 |

## 七、最终验收链

本地开发 → 本地测试 PASS → 交叉编译 PASS → 板端安装 PASS → 真机测试 PASS →
性能测试 PASS → 稳定性测试 PASS（30 分钟连续跑，按你的口径推到镜像做完之后）→ 发布包。

上板实测要看到的硬证据（沿用上次走通那套）：5 个 unit 全 `enabled` + `active`、
`/api/license` 返回 `activated:true`、HDMI `/dev/video0` STREAMON OK、
`/api/core/status` 有采集与预览帧率、`raw-gadget` + `UDC=fc000000.usb state=configured`。

## 八、开工后怎么走

你说"可以开始了"，我从 **S1** 起步。每完成一步我把证据（命令输出、测试结果、面板实测）
摆给你看，你点头才进下一步。

---

## 九、S1 已实施并通过审查（2026-09-18 补记）

**S1「减法清场」已经做完并被独立审查通过。** 审查报告：`docs/交付前S1代码审查-2026-09-18.md`。

改动规模：18 个文件 + 7 处暂存删除，**+360 / −2554**。我独立复现的验收结果：

| 项 | 结果 |
|----|------|
| `ttbox-web.py` 路由 | 112 → **90**（删 22 条，逐条核对无误；`ota/install` 去重也落实了） |
| 面板页签 | 12 → **9**，重编为连续 01–09（总览/热键控制/移动控制/辅助功能/模型库/显示与鼠标/预设参数/系统状态/风扇控制） |
| core | `MakcuMouseBackend.{cpp,hpp}` 删除、CMakeLists 摘除、`OutputBackend.cpp` 摘 include、`Application.cpp` 4 个 kind 分支改为"显式告警 + 落 aibox 兜底" |
| payload 排除 | `platform/`、`wifi_manager.py`、`api_v1.py`、`framework_api.py`、`static/legacy/` 移出；`--exclude=tests` 与 install 侧镜像一致（E02 落实） |
| 仓库清理 | 根 `third_party/`(2)、3 个 `_probe_*.txt`、`out/`(15M) 已删 |
| ctest | **28/28 passed**（ASCII 路径 `core/build-j`） |
| pytest | **186 passed** |
| 口径门禁 | **PASS**（⑨ 项全绿） |
| 产物反查 | 两个 build 目录的 `Application.cpp.obj` 均含 S1 新增的 `A801` 串 ⇒ 确为改后源码所编 |

**结论：0 条阻塞项。** 审查提出的 12 条（N01–N12）已全部登记进
`docs/交付前逐项定案台账-2026-09-18.md` 的「S1 执行与审查」一节。

### 进 S2 前建议先做两件（都是一行/一次命令的事）

1. **N06**：`git add -A` 把 17+7 处改动一次原子提交 —— 现在 core 已 staged、web/脚本/测试 18 个文件未 staged，
   若直接 `git commit` 会提交出"core 删了、web 没删"的破状态。
2. **N01**：删掉 `plugins/web/__init__.py:1` 的 `from .framework_api import install_framework_api` ——
   该模块已从出货包排除，出货包内任何 `import plugins.web` 都会 ImportError（当前无调用方，属潜伏陷阱）。

### 一条环境红线（避免白折腾）

跑 ctest 一律用 **`C:\ttbox-local\core\build-j`**（`C:\ttbox-local` 是指向本工作区的符号链接，
路径纯净 ASCII）。`core/build-host` 是在中文路径下配置的，它把 rknn 头文件路径烧成了 UTF-8 中文字节，
MinGW 窄字符文件 API 打不开 ⇒ 必有 2 个读文件用例假失败（`input_quant_enum_mirror_against_real_header`、
`config_loads_existing_default_json`）。这不是回归，我已用最小实验坐实。

