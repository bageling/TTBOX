# 面板功能补齐实施计划（2026-09-19）

背景：《交付前Web按钮落实审计-2026-09-19》第二档列出的一批"面板有控件、Core 没有概念"
的功能，用户决定**补实现**（在 Core 里加字段与算法），不是从面板摘掉。

本文件是实施计划与分批依赖表，随实施推进更新；每批完成后回填"结果"。

---

## 一、规格来源

行为规格以**面板现有控件为准**——字段名、取值范围、默认值、提示文案，
前端 `plugins/web/templates/index.html` 里已经写全了，这就是契约，不再另立一套。

| 功能 | 面板锚点 | 默认值常量 |
|------|----------|-----------|
| 自动开火 | `assist-section-trigger`（index.html:5597）+ `autoTriggerProfileTemplate`（:9092） | `AUTO_TRIGGER_DEFAULTS`（:6412） |
| 连点 | `assist-section-rapid`（:5610） | `RAPID_FIRE_DEFAULTS`（:6425） |
| 自动背闪 | `assist-section-back-flick`（:5629） | `AUTO_BACK_FLICK_DEFAULTS`（:6448） |
| 准星找色 | `assist-section-crosshair`（:5653） | `CROSSHAIR_DEFAULTS`（:6431） |
| 风扇调速 | `fan_control_*`（:6071） | `FAN_CONTROL_DEFAULTS`（:6439） |
| 热键保护 | `hotkey_guard_*`（:5410） | `HOTKEY_GUARD_DEFAULTS`（:6461） |
| ki 积分项 | `controller_ki_x` / `controller_ki_y`（:5630 附近） | —（PD 体系无默认值） |

### 已确认的两条关键语义

1. **自动开火**（面板提示原文，index.html:5602）：
   "每条配置独立决定按住热键和目标类别；**准星进入匹配目标框内即可触发**。"
   ⇒ 触发条件 = `按住热键` ∧ `准星坐标落在匹配类别目标的框内`（再叠进入延迟与冷却）。
   ⇒ **准星找色是自动开火的前置**，两者不能颠倒实现。
2. **连点**（面板提示原文，:5617-5620）：
   "按住该键时循环模拟左键点击"；`press_base_ms`=170、`interval_base_ms`=13，
   "实际会加入随机浮动，偶尔会慢一拍"。

### 需要新增的底座能力

`自动开火` 与 `连点` 都要往 HID 注入**按键**，而当前 Core 做不到：

- `core/src/output/MouseControlClient.hpp:37` 只有 `send_move()`，**没有按键命令**；
- `core/src/output/OutputBackend.cpp:56` 的 `UsbProxyMouseControlBackend::mouse_button()`
  是个空壳，只 `return gate_allows()`，什么都没发；
- `core/src/output/LocalHidBackend.cpp:101` 的 `mouse_button()` 同理，注释写着"保留接口"。

usb-proxy 侧**已经支持**了：`usbproxy/mouse_control.cpp` 的 `kButtonCmd(5)`
载荷 `<BB button,action>`，button 1~8、action ∈ {down=1,up=2,click=3}。
所以底座这一层是"把已有的协议接上"，不是新造。

---

## 二、分批与依赖

```
T2-0 纯接线（web 侧，无 Core 改动）        ── 可独立交付
T2-1 按键注入底座（Core ↔ usb-proxy）      ── 以下三项的前置
   ├── T2-2 Pid1Controller 加 ki 积分项    ── 独立，不依赖 T2-1
   ├── T2-3 连点       ── 依赖 T2-1
   ├── T2-6 准星找色   ── 独立（检测侧）
   │      └── T2-4 自动开火 ── 依赖 T2-1 + T2-6
   └── T2-5 自动背闪   ── 依赖 T2-1（要发位移 + 要类别过滤）
T2-7 风扇调速（Application.cpp 开机满转段改闭环） ── 独立
T2-8 热键保护 hotkey_guard（保存 + 回读 + 热键挂起真值） ── 可并入 T2-0
```

### T2-0 纯接线（web 侧）

- **ONNX 导入前端接线**：前端固定打 `/api/models/import`，被 ttbox-web.py:2674-2679 拒；
  后端转换链（`_run_onnx_conversion`）已实现。改前端按扩展名分流到转换端点。
- **预设导出产出真文件**：现在 `/api/presets/export` 返回 API 信封而非文件本体，
  "导出→导入→加载"恒静默无效。改成返回可再导入的文件（`send_file` + 正确
  `Content-Disposition`），并修 `import_preset`（ttbox-web.py:2963）忽略名称字段的问题。
- **hotkey_guard 打通**：保存入 Core、回读真实值、徽标用真值；
  `collect_web_state` 的 `hotkeys_suspended`（ttbox-web.py:1279）改成读真实挂起状态。

输出物：`plugins/web/bin/ttbox-web.py`、`plugins/web/templates/index.html`、测试文件。
验证方式：pytest 新增用例（保存往返、导出文件可再导入、挂起状态与真值一致）+
反向验证（改回旧实现应转红）。

### T2-1 按键注入底座

- `MouseControlClient` 加 `send_button(uint8_t button, uint8_t action)`，
  按 `mouse_control.cpp` 的 `kButtonCmd` 载荷封装（`<BB>`，头 8 字节 + 载荷）。
- `UsbProxyMouseControlBackend::mouse_button()` 改成真发命令，返回值反映真实结果。
- `LocalHidBackend::mouse_button()` 按 gadget report 填 button 位（本机 HID 路径）。

输出物：`core/src/output/MouseControlClient.{hpp,cpp}`、`OutputBackend.{hpp,cpp}`、
`LocalHidBackend.{hpp,cpp}`、`core/tests` 新增单测。
验证方式：Core 单测（协议字节对齐 usbproxy 解码顺序）+ 板端实测
（发 click 后观察 Windows 端是否真收到左键）。

### T2-2 Pid1Controller 加 ki 积分项

- `MouseProfile` 加 `ki_x` / `ki_y`（默认 0）。
- `Pid1Controller` 加积分累加 + 抗积分饱和（输出 clamp 时停止累积）。
- **约定：默认 ki=0 ⇒ 输出与当前逐字节一致**，照 `ContinuousLeadConfig` 的先例
  （见 MouseTypes.hpp:242-246 的注释），保证"没开就等于没加"。

输出物：`core/src/aim/Pid1Controller.hpp`、`core/src/mouse/MouseTypes.hpp`、
web 翻译层（`CONTROLLER_NUMS` 加两个键）+ 回读。
验证方式：Core 单测（ki=0 输出不变 / ki>0 消除稳态误差 / 饱和不累积）。

### T2-3 连点 rapid_fire（依赖 T2-1）

字段：`enabled` / `hotkey` / `press_base_ms`(170) / `interval_base_ms`(13)。

行为（待确认的随机模型，我按这个实现）：
按住 `hotkey` 时循环 —— 按下左键并保持 `press_base_ms × U(0.85,1.15)`；
松开；等待 `interval_base_ms × U(1.0,1.6)`，其中每 ~8 次插入一次"慢一拍"
（延长到 `interval_base_ms × U(2.5,3.5)`），对应提示里的"偶尔会慢一拍"。
松开 `hotkey` 立即停止并释放按键。

### T2-4 自动开火 auto_trigger（依赖 T2-1 + T2-6）

每条 profile：`hotkey`（按住热键）/ `fire_mode`（tap 点射 | spray 扫射）/
目标类别掩码 / `enter_delay_min..max_ms`（0-500 随机）/
`cooldown_min..max_ms`（50-3000 随机）/ `spray_release_delay_ms`(500) /
`continuous_mode` / `spray_recoil_assist`。

行为：
- 触发条件 = 热键按住 ∧ 准星落在匹配类别目标框内 ∧ 距上次开火超过冷却。
- 首次进入：等待 `enter_delay` 随机值再按下。
- `tap`：按下 → 松开（短点），冷却随机后再判。
- `spray`：按下并保持，到 `spray_release_delay_ms` 松开；
  `continuous_mode` = 只要条件成立就不停循环；`spray_recoil_assist` = 扫射期间叠加压枪。

### T2-5 自动背闪 auto_back_flick

字段齐全（`turn_pixels`3000 / `wait_ms`150 / `steps`100 / `cooldown_ms`3000 /
`turn_random`50 / `return_random`20 / `random_direction` / `dodge_away_from_target` /
类别掩码）。
行为：检测到匹配类别目标 → 横向移动 `turn_pixels`（分 `steps` 步平滑，方向按
`random_direction` 或按目标位置反闪）→ 停 `wait_ms` → 分 `steps` 步回正 →
冷却 `cooldown_ms`。位移通过既有输出链发（`send_move`），**不依赖 T2-1**，
但保留依赖声明是因为要读类别过滤。

### T2-6 准星找色 crosshair

字段：`detection_enabled` / `only_when_fire_held` / `fire_hotkey` /
`roi_w`、`roi_h`（2-150，围绕画面中心）/ 多个色彩槽（每槽 `enabled` + 一种颜色
∈ {red,green,blue,cyan,white,black}）。

行为：在中心 `roi_w × roi_h` 区域按启用槽的颜色做像素匹配，得到准星坐标；
`only_when_fire_held` 开启时只在按住 `fire_hotkey` 时输出结果。
输出供 T2-4 判定"准星是否在目标框内"；未检测到则回落到画面中心。

实现位置：`core/src/pipeline/`（`AimTargetTask.hpp:19-20` 已有死声明
`crosshair_detected` / `AimPoint crosshair`，正好接上）。

### T2-7 风扇调速 fan_control

字段：`enabled` / `temperature_source`(auto) / `start_celsius`(45) / `full_celsius`(70) /
`min_pwm_percent`(100) / `max_pwm_percent`(100) / `stop_hysteresis_celsius`(3)。

行为：温度 ≤ `start` → `min_pwm`；≥ `full` → `max_pwm`；中间线性插值；
降到 `start - 滞回` 以下才回到 `min_pwm`。
改造点：`core/src/app/Application.cpp:456-465` 现在是无条件 `pwm << 255`，改成
启动时读配置、按需闭环；`enabled=false` 时保持现状（满转，防热节流）。

### T2-8 热键保护 hotkey_guard

字段：`enabled` / `toggle_hotkey`(middle)。
行为：按一次 `toggle_hotkey` 在"热键生效 / 全部挂起"之间切换；
挂起状态经 `/api/state` 的 `state.aim.hotkeys_suspended` 如实回读。

---

## 三、每批的统一验证流程（RK3588 固定流程，不可裁剪）

1. Windows 本地改码 → 本地完整测试（编译 → 单测 → 功能 → 接口 → 回归）；
   本地没 PASS 不许上板。
2. 本地 PASS 后进 WSL 交叉编译 ARM64 → 检查架构/依赖/版本/打包。
3. 上板真机测试：只验真实硬件与真实运行环境（HID 注入、RTP 采集、类别过滤）。
4. 板端发现 Bug → 回 Windows 改 → 重跑 1~3，绝不在板端改正式源码。
5. 全部通过才算该批完成。

---

## 四、当前状态

| 批 | 状态 |
|----|------|
| T2-0 纯接线 | 未开始 |
| T2-1 按键注入底座 | 未开始 |
| T2-2 ki 积分项 | 未开始 |
| T2-3 连点 | 未开始 |
| T2-4 自动开火 | 未开始 |
| T2-5 自动背闪 | 未开始 |
| T2-6 准星找色 | 未开始 |
| T2-7 风扇调速 | 未开始 |
| T2-8 热键保护 | 未开始 |

第一档（去假，四处撒谎）已完成，提交 `fe8b9fa`，见
`docs/交付前Web按钮落实审计-2026-09-19.md`。
