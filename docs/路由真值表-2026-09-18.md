# TTBOX Web 路由真值表（2026-09-18）

> 出货前逐条定案核对表。三个路由来源：`plugins/web/bin/ttbox-web.py`（主路由层）、`plugins/web/framework_api.py`（Framework 兼容层）、`plugins/web/api_v1.py`（第二套 /api/v1）。
> 前端调用方判定基准 = `plugins/web/templates/index.html`（唯一现役前端）；`static/legacy/app.js` 已停用；`static/designer.js` 属默认关闭页（TTBOX_ENABLE_DESIGNER）。

## 零、统计总览

| 文件 | 路由总数 | 真实实现(有前端调用) | 死端点(实现完整但零调用) | 纯桩 | 未实现(下沉T2.x) | 平台错配 |
|---|---|---|---|---|---|---|
| bin/ttbox-web.py | 112 | 62 | 33 | 6 | 4 | 2（与死端点交叉） |
| framework_api.py | 26 | 0 | 26 | 0 | 0 | 0 |
| api_v1.py | 47 | 3（designer/css×3） | 44（含 28 条桩） | 28 | 0 | 0 |
| **合计** | **185** | **65** | **103** | **34** | **4** | **2** |

前端调用点取证：① 全量抓取 index.html 中引号包裹的 `/api/...` 完整 URL → 79 处调用点；② 缺失端点关键词 alternation 复核（详见各行备注 grep 字样）→ 除 1 处 HTML label 文本外全部零命中；③ 非 fetch 形式专项复核（`<a href>` / `img src` / `window.open` / `form action` / `EventSource` / `location.href`）→ 命中 4 处，均已计入下表（#1、#4、#50、#112）。

## 一、bin/ttbox-web.py（112 条）

### 页面（4）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | / | ttbox-web.py:1710 | 真实 | index.html:7490(window.location.replace→"/activate" 重定向回 '/'), 11649(改端口后跳 `http://host:port/`) | 非 fetch 调用；render index.html |
| GET | /desktop | ttbox-web.py:1716 | 真实 | 浏览器直入 | 与 / 同模板 + mode=desktop |
| GET | /mobile | ttbox-web.py:1723 | 真实 | 浏览器直入 | 复用 index.html + mode=mobile |
| GET | /activate | ttbox-web.py:1732 | 真实 | index.html:7479 activationPageUrl() 返回 "/activate" → 7490 window.location.replace | 非 fetch 调用；render activate.html |

### 系统/状态（15）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/health/frontend | ttbox-web.py:1750 | 真实 | 零调用 | index.html grep 'health/frontend' → 0；调用方=framework_api.py:57 插件探活 |
| GET | /api/state | ttbox-web.py:1755 | 真实 | index.html:12380, 14423, 15983 | 前端状态主数据源 |
| GET | /api/announcement | ttbox-web.py:1760 | 未实现 | index.html:7417 | ★已定性：恒 503（无公告源），前端 catch 吞掉、静默失败 |
| GET | /api/system | ttbox-web.py:1768 | 真实 | index.html:12264 | 系统统计 |
| GET | /api/system/version | ttbox-web.py:1774 | 死端点 | 零调用 | grep 'system/version' → 0；前端版本来自 /api/state |
| GET | /api/system/storage | ttbox-web.py:1786 | 真实 | index.html:11454 | |
| POST | /api/system/storage/expand | ttbox-web.py:1816 | 真实 | index.html:11474 | |
| PUT | /api/system/hostname | ttbox-web.py:1825 | 真实 | index.html:11627 | |
| PUT | /api/system/web-port | ttbox-web.py:1838 | 真实 | index.html:11638 | |
| GET | /api/system/lan-blocklist | 已删（D04 整块下线） | 已删 | 已删 | 2026-09-18 |
| POST | /api/system/lan-blocklist/scan | 已删（D04 整块下线） | 已删 | 已删 | 2026-09-18 |
| POST | /api/system/lan-blocklist | 已删（D04 整块下线） | 已删 | 已删 | 2026-09-18 |
| DELETE | /api/system/lan-blocklist | 已删（D04 整块下线） | 已删 | 已删 | 2026-09-18 |
| POST | /api/system/reactivate | ttbox-web.py:1948 | 未实现 | index.html:15885 | 409 下沉 T2.x；前端有调但恒失败 |
| POST | /api/system/master-reactivate | ttbox-web.py:1960 | 未实现 | 零调用 | ★已定性；grep 'master-reactivate' → 0；与 :1948 逐字重复 |

### OTA/更新（8）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| POST | /api/ota/install | ttbox-web.py:1970 | 死端点 | 零调用 | grep '/api/ota/install'（79 处全量调用点）→ 0；函数体与 _ota_install_impl()(2000-2015) 逐字相同 |
| POST | /api/update/install | ttbox-web.py:2018 | 真实 | index.html:12824 | 透传 _ota_install_impl() |
| GET | /api/update/status | ttbox-web.py:2024 | 真实 | index.html:12717 | systemctl 单元状态投影 |
| GET | /api/update/versions | ttbox-web.py:2046 | 纯桩 | index.html:12581 | ★已定性：恒 400 not_supported |
| GET | /api/update/check | ttbox-web.py:2052 | 纯桩 | index.html:12807 | ★已定性：恒 400 not_supported |
| POST | /api/update/cleanup-stuck | ttbox-web.py:2057 | 纯桩 | index.html:12853 | ★已定性：恒 400 not_supported |
| POST | /api/system/reboot | ttbox-web.py:2062 | 真实 | index.html:15078 | |
| POST | /api/system/poweroff | ttbox-web.py:2071 | 真实 | index.html:15086 | |

### 配置/设置（4）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| PUT | /api/config | ttbox-web.py:2097 | 真实 | index.html:11149 | IPC SET_CONFIG |
| GET | /api/config | ttbox-web.py:2126 | 死端点 | 零调用 | grep '/api/config' 全量仅 11149（PUT）→ GET 零调用；前端读 /api/state |
| GET | /api/settings/auto-start | ttbox-web.py:2257 | 死端点 | 零调用 | grep 'auto-start' → 0 |
| PUT | /api/settings/auto-start | ttbox-web.py:2262 | 死端点 | 零调用 | grep 'auto-start' → 0；实现真实（systemctl enable/disable ttbox-core/web） |

### 模型（14）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/models | ttbox-web.py:2435 | 真实 | index.html:14782, 14971, 14997, 15440 | |
| GET | /api/models/convert-status | ttbox-web.py:2616 | 死端点 + 平台错配 | 零调用 | grep 'convert-status' → 0；转换器 /opt/ttbox/tools/converter 不在 payload 拷贝清单（fhs_init.sh:350-407 无 tools/） |
| POST | /api/models/import-onnx | ttbox-web.py:2622 | 死端点 + 平台错配 | 零调用 | grep 'import-onnx' → 0；'onnx' 全量命中 8734-15439 均为 UI 文案/类型判断；UI 恒 POST /api/models/import(15433)；转换器不随包 |
| GET | /api/models/device-code | ttbox-web.py:2668 | 真实 | index.html:14735 | 读 /proc/cpuinfo |
| POST | /api/models/cloud-encrypted | ttbox-web.py:2689 | 纯桩 | 零调用 | ★已定性：恒 503 '未接入云端模型服务'；grep 'cloud-encrypted' → 0 |
| POST | /api/models/import | ttbox-web.py:2703 | 真实 | index.html:15433 | ⚠ 契约错配：UI 允许选 ONNX 且提示"转换中"(15426-15439)，但板端只收 .rknn（除非 TTBOX_ALLOW_ONNX=1）→ 板上必报"仅支持 .rknn" |
| POST | /api/models/delete | ttbox-web.py:2761 | 真实 | index.html:14777 | |
| POST | /api/models/select | ttbox-web.py:2773 | 真实 | index.html:14691 | |
| POST | /api/models/bind-preset | ttbox-web.py:2811 | 真实 | index.html:14754 | |
| POST | /api/models/game-profile | ttbox-web.py:2825 | 死端点 | 零调用 | grep 'game-profile' → 0 |
| POST | /api/models/remote-frame-format | ttbox-web.py:2839 | 死端点 | 零调用 | grep 'remote-frame' → 0 |
| POST | /api/models/rknn-concurrency | ttbox-web.py:2855 | 死端点 | 零调用 | grep 'rknn-concurrency' → 0 |
| POST | /api/models/hailo-pipeline-depth | ttbox-web.py:2889 | 死端点 | 零调用 | grep 'hailo-pipeline' → 0 |
| POST | /api/models/class-names | ttbox-web.py:2906 | 真实 | index.html:14870 | |

### 预设（5）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/presets | ttbox-web.py:2924 | 真实 | index.html:14966, 15445, 15481, 15514 | |
| POST | /api/presets | ttbox-web.py:2932 | 真实 | index.html:13369, 14958, 15475 | |
| POST | /api/presets/load | ttbox-web.py:2966 | 真实 | index.html:14936 | |
| POST | /api/presets/import | ttbox-web.py:2999 | 真实 | index.html:15507 | |
| GET | /api/presets/<name>/export | ttbox-web.py:3017 | 真实 | index.html:8815(presetExportUrl 定义) + 13503(`<a href target=_blank>`) | 非 fetch 调用（<a href> 导出链接），不算零调用 |

### 控制/诊断（10）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/control/calibration | ttbox-web.py:3508 | 死端点 | 零调用 | grep 'calibration' → 仅 5783 HTML label 文本（modelCalibrationZip 表单字段名），无 /api/control/calibration |
| PUT | /api/control/calibration | ttbox-web.py:3513 | 死端点 | 零调用 | 同上 |
| POST | /api/control/calibration/start | ttbox-web.py:3558 | 死端点 | 零调用 | 同上 |
| POST | /api/control/calibration/cancel | ttbox-web.py:3575 | 死端点 | 零调用 | 同上 |
| DELETE | /api/control/calibration | ttbox-web.py:3587 | 死端点 | 零调用 | 同上 |
| POST | /api/control/start | ttbox-web.py:3593 | 真实 | index.html:15057 | |
| POST | /api/control/stop | ttbox-web.py:3610 | 真实 | index.html:15057 | |
| POST | /api/diagnostics/aim-trace | ttbox-web.py:3622 | 真实 | index.html:15192 | |
| GET | /api/diagnostics/usb-proxy.zip | ttbox-web.py:3675 | 真实 | index.html:7553 | fetch 下载 |
| GET | /api/events | ttbox-web.py:3701 | 纯桩 + 死端点 | 零调用 | grep '/api/events' → 0、'EventSource' → 0；固定返回 {'events':[]} |

### 硬件（6）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/hardware/mouse | ttbox-web.py:3719 | 真实 | index.html:14416, 15834 | |
| PUT | /api/hardware/mouse | ttbox-web.py:3885 | 真实 | index.html:15834 | |
| PUT | /api/hardware/mouse/mode | ttbox-web.py:3912 | 真实 | index.html:7369, 15863 | |
| PUT | /api/hardware/mouse/timing | ttbox-web.py:3931 | 死端点 | 零调用 | grep 'mouse/timing' → 0 |
| GET | /api/hardware/display | ttbox-web.py:3973 | 真实 | index.html:7354, 14415, 15808 | |
| PUT | /api/hardware/display | ttbox-web.py:4127 | 真实 | index.html:15808 | |

### 网络（6）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/network/wifi | ttbox-web.py:4209 | 真实 | index.html:12154 | |
| POST | /api/network/wifi/scan | ttbox-web.py:4214 | 真实 | index.html:12153 | |
| POST | /api/network/wifi/connect | ttbox-web.py:4219 | 真实 | index.html:12201 | |
| POST | /api/network/wifi/fallback | ttbox-web.py:4232 | 真实 | index.html:12220 | |
| POST | /api/network/wifi/ap/apply | ttbox-web.py:4240 | 真实 | index.html:12229 | |
| POST | /api/network/wifi/client/activate | ttbox-web.py:4250 | 真实 | index.html:12249 | |

### 授权（6）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/license | ttbox-web.py:4328 | 真实 | index.html:12324, 12369；activate.html:327 | |
| POST | /api/license/activate | ttbox-web.py:4333 | 真实 | index.html:12406；activate.html:353 | |
| POST | /api/activation/network/prepare | ttbox-web.py:4438 | 死端点 | 零调用 | grep 'network/prepare' → 0；半桩：读真 wifi 状态但 attempted/changed 固定 true/false |
| POST | /api/activation/reset-local-identity | ttbox-web.py:4456 | 未实现 | 零调用 | grep 'reset-local' → 0；409 下沉 T2.x |
| GET | /api/activation/full-recovery | ttbox-web.py:4468 | 死端点 | 零调用 | grep 'full-recovery' → 0 |
| POST | /api/activation/full-recovery | ttbox-web.py:4493 | 未实现 | 零调用 | grep 'full-recovery' → 0；409 下沉 T2.x |

### Hailo/主题/品牌（12）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/hailo/status | ttbox-web.py:4504 | 真实 | index.html:14340 | ★已定性 |
| POST | /api/hailo/install | ttbox-web.py:4559 | 真实 | index.html:14369 | ★已定性 |
| GET | /api/themes | ttbox-web.py:4568 | 死端点 | 零调用 | grep '/api/themes' → 0 |
| GET | /api/themes/<theme_id>/previews/<int:index> | ttbox-web.py:4593 | 死端点 | 零调用 | grep '/api/themes' → 0 |
| POST | /api/themes/redeem | ttbox-web.py:4598 | 死端点 | 零调用 | grep '/api/themes' → 0 |
| POST | /api/themes/<theme_id>/install | ttbox-web.py:4607 | 死端点 | 零调用 | grep '/api/themes' → 0 |
| PUT | /api/themes/current | ttbox-web.py:4618 | 死端点 | 零调用 | grep '/api/themes' → 0 |
| GET | /theme-assets/<theme_id>/<version>/<path:filename> | ttbox-web.py:4628 | 死端点 | 零调用 | grep 'theme-assets' → 0 |
| GET | /api/branding/background | ttbox-web.py:4638 | 死端点 | 零调用 | grep 'branding' → 0 |
| POST | /api/branding/background | ttbox-web.py:4643 | 死端点 | 零调用 | grep 'branding' → 0 |
| DELETE | /api/branding/background | ttbox-web.py:4653 | 死端点 | 零调用 | grep 'branding' → 0 |
| GET | /api/branding/background/image | ttbox-web.py:4658 | 死端点 | 零调用 | grep 'branding' → 0 |

### 动作曲线（12，整组死端点）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/motion-profiles | ttbox-web.py:4696 | 死端点 | 零调用 | grep 'motion-profile' → 0；实现真实（读 MOTION_PROFILES_DIR） |
| POST | /api/motion-profiles | ttbox-web.py:4704 | 死端点 | 零调用 | grep 'motion-profile' → 0 |
| DELETE | /api/motion-profiles/<profile_id> | ttbox-web.py:4719 | 死端点 | 零调用 | grep 'motion-profile' → 0 |
| GET | /api/motion-profiles/<profile_id>/export | ttbox-web.py:4727 | 死端点 | 零调用 | grep 'motion-profile' → 0 |
| POST | /api/motion-training/sessions | ttbox-web.py:4739 | 死端点 | 零调用 | grep 'motion-training' → 0 |
| PUT | /api/motion-training/sessions/<session_id>/heartbeat | ttbox-web.py:4752 | 死端点 | 零调用 | grep 'motion-training' → 0 |
| POST | /api/motion-training/sessions/<session_id>/samples | ttbox-web.py:4760 | 死端点 | 零调用 | grep 'motion-training' → 0 |
| DELETE | /api/motion-training/sessions/<session_id> | ttbox-web.py:4769 | 死端点 | 零调用 | grep 'motion-training' → 0 |
| POST | /api/motion-profiles/<profile_id>/train | ttbox-web.py:4777 | 死端点 | 零调用 | grep 'motion-profile' → 0 |
| POST | /api/motion-profiles/<profile_id>/activate | ttbox-web.py:4785 | 死端点 | 零调用 | grep 'motion-profile' → 0 |
| DELETE | /api/motion-profiles/active | ttbox-web.py:4796 | 死端点 | 零调用 | grep 'motion-profile' → 0 |
| DELETE | /api/motion-profiles/<profile_id>/samples | ttbox-web.py:4806 | 死端点 | 零调用 | grep 'motion-profile' → 0 |

### 外设/预览（10）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| POST | /api/remote/connect | ttbox-web.py:4823 | 死端点 | 零调用 | ★已定性；grep '/api/remote' → 0 |
| GET | /api/remote/models | ttbox-web.py:4828 | 死端点 | 零调用 | ★已定性；grep '/api/remote' → 0 |
| POST | /api/remote/import | ttbox-web.py:4833 | 死端点 | 零调用 | ★已定性；grep '/api/remote' → 0 |
| POST | /api/remote/delete | ttbox-web.py:4838 | 死端点 | 零调用 | ★已定性；grep '/api/remote' → 0 |
| GET | /api/makcu/devices | ttbox-web.py:4844 | 真实 | index.html:7962 | ★已定性（有调） |
| GET | /api/ferrum/devices | ttbox-web.py:4870 | 真实 | index.html:8066 | ★已定性（有调） |
| GET | /api/kmboxb/devices | ttbox-web.py:4875 | 死端点 | 零调用 | ★已定性；grep 'kmboxb' → 0 |
| POST | /api/mouse-output/test-circle | ttbox-web.py:4880 | 真实 | index.html:8351 | ★已定性（有调） |
| GET | /api/preview.jpg | ttbox-web.py:4890 | 死端点 | 零调用 | grep 'preview.jpg' → 仅命中 11254 的 .mjpg 行，无 .jpg；前端只用 mjpg |
| GET | /api/preview.mjpg | ttbox-web.py:4915 | 真实 | index.html:11254（赋给 preview.src，img#previewImage@5279） | 非 fetch 调用（img src） |

## 二、framework_api.py（26 条）—— 整文件对 index.html 零消费

实现全部真实（PluginManager / LocalRepository / SystemPluginHost / IPC）。index.html 复核 grep：'/api/plugins'、'core/status'、'model/list'、'model/active'、'system/status'、'network/status'、'wifi/status'、'fan/status'、'monitor/status'、'upgrade/status'、'hwmon' 全部 → 0。仅 framework/tests/test_web_framework_api.py:23-31 在测。

| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/plugins | framework_api.py:49 | 死端点 | 零调用 | grep '/api/plugins' → 0；真实（discover+HTTP 探活同步） |
| GET | /api/plugins/<plugin_id> | framework_api.py:73 | 死端点 | 零调用 | 同上 |
| GET | /api/plugins/<plugin_id>/status | framework_api.py:79 | 死端点 | 零调用 | 同上 |
| POST | /api/plugins/<plugin_id>/enable | framework_api.py:89-91 | 死端点 | 零调用 | add_url_rule 批量注册 |
| POST | /api/plugins/<plugin_id>/disable | framework_api.py:89-91 | 死端点 | 零调用 | 同上 |
| POST | /api/plugins/<plugin_id>/start | framework_api.py:89-91 | 死端点 | 零调用 | 同上 |
| POST | /api/plugins/<plugin_id>/stop | framework_api.py:89-91 | 死端点 | 零调用 | 同上 |
| POST | /api/plugins/<plugin_id>/restart | framework_api.py:89-91 | 死端点 | 零调用 | 同上 |
| POST | /api/plugins/<plugin_id>/rollback | framework_api.py:89-91 | 死端点 | 零调用 | 同上 |
| POST | /api/plugins/<plugin_id>/uninstall | framework_api.py:89-91 | 死端点 | 零调用 | 同上 |
| POST | /api/plugins/install | framework_api.py:93 | 死端点 | 零调用 | grep '/api/plugins' → 0 |
| POST | /api/plugins/upgrade | framework_api.py:103 | 死端点 | 零调用 | 同上 |
| GET | /api/plugins/market | framework_api.py:112 | 死端点 | 零调用 | 同上（与 /api/plugins 非重复：列表 vs 本地市场） |
| GET | /api/plugins/market/<plugin_id> | framework_api.py:118 | 死端点 | 零调用 | 同上 |
| GET | /api/plugins/market/<plugin_id>/versions | framework_api.py:124 | 死端点 | 零调用 | 同上 |
| GET | /api/system/status | framework_api.py:132-134 | 死端点 | 零调用 | grep 'system/status' → 0；SystemPluginHost |
| GET | /api/network/status | framework_api.py:132-134 | 死端点 | 零调用 | grep 'network/status' → 0 |
| GET | /api/wifi/status | framework_api.py:132-134 | 死端点 | 零调用 | grep 'wifi/status' → 0 |
| GET | /api/fan/status | framework_api.py:132-134 | 死端点 | 零调用 | grep 'fan/status' → 0 |
| GET | /api/monitor/status | framework_api.py:132-134 | 死端点 | 零调用 | grep 'monitor/status' → 0 |
| GET | /api/upgrade/status | framework_api.py:132-134 | 死端点 | 零调用 | grep 'upgrade/status' → 0 |
| GET | /api/core/status | framework_api.py:136 | 死端点 | 零调用 | grep 'core/status' → 0；真实 IPC GET_STATUS，前端用 /api/state |
| GET | /api/model/list | framework_api.py:173 | 死端点 | 零调用 | grep 'model/list' → 0；真实 IPC MODEL_LIST，前端用 /api/models |
| GET | /api/model/active | framework_api.py:191 | 死端点 | 零调用 | grep 'model/active' → 0 |
| GET | /api/hwmon | framework_api.py:200 | 死端点 | 零调用 | grep 'hwmon' → 0 |

## 三、api_v1.py（47 条，url_prefix=/api/v1，ttbox-web.py:1651 注册蓝图）

index.html 对 '/api/v1' grep → 0。唯一消费方 = static/designer.js:9（'/api/v1/designer/css'）。已定"移出出货包"。

### 已实现（真实 IPC 真接线，15）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/v1/health | api_v1.py:213 | 真实 | 零调用 | grep '/api/v1' → 0 |
| GET | /api/v1/status | api_v1.py:219 | 真实 | 零调用 | 同上 |
| GET | /api/v1/config | api_v1.py:225 | 真实 | 零调用 | 重复 GET /api/config(ttbox-web.py:2126，亦零调用) |
| PUT | /api/v1/config | api_v1.py:242 | 真实 | 零调用 | 重复 PUT /api/config(2097，有调) |
| POST | /api/v1/runtime/<action> | api_v1.py:286 | 真实 | 零调用 | start/stop/restart；重复 /api/control/start|stop(3593,3610) |
| GET | /api/v1/models | api_v1.py:294 | 真实 | 零调用 | 重复 GET /api/models(2435) |
| POST | /api/v1/models/activate | api_v1.py:300 | 真实 | 零调用 | 重复 /api/models/select(2773) |
| POST | /api/v1/models/validate | api_v1.py:309 | 真实 | 零调用 | 主路由无对应 |
| POST | /api/v1/models/install | api_v1.py:318 | 真实 | 零调用 | 重复 /api/models/import 收尾链(MODEL_IMPORT→VALIDATE→INSTALL) |
| POST | /api/v1/models/remove | api_v1.py:327 | 真实 | 零调用 | 重复 /api/models/delete(2761) |
| POST | /api/v1/models/set-concurrency | api_v1.py:336 | 真实 | 零调用 | 重复 /api/models/rknn-concurrency(2855，亦零调用) |
| POST | /api/v1/models/import | api_v1.py:355 | 真实 | 零调用 | 重复 /api/models/import(2703)；src_path 版 vs multipart 版 |
| GET | /api/v1/preview.jpg | api_v1.py:387 | 真实 | 零调用 | 重复 GET /api/preview.jpg(4890，亦零调用) |
| GET | /api/v1/preview.mjpg | api_v1.py:420 | 真实 | 零调用 | 重复 /api/preview.mjpg(4915，有调) |
| GET | /api/v1/capabilities | api_v1.py:613 | 真实 | 零调用 | 能力清单唯一真源（文档层） |

### NOT_IMPLEMENTED 桩路由（_register_not_implemented @548-560 按 NOT_IMPLEMENTED_SPECS(470-528) 批量注册，28 条；HTTP 200 + ok:false + code:NOT_IMPLEMENTED；全部前端零调用，grep '/api/v1' → 0）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/v1/system/version | api_v1.py:472(spec) | 纯桩 | 零调用 | spec@472 |
| GET | /api/v1/system/storage | api_v1.py:474(spec) | 纯桩 | 零调用 | spec@474 |
| POST | /api/v1/system/reboot | api_v1.py:476(spec) | 纯桩 | 零调用 | spec@476 |
| POST | /api/v1/system/poweroff | api_v1.py:478(spec) | 纯桩 | 零调用 | spec@478 |
| GET | /api/v1/system/hostname | api_v1.py:480(spec) | 纯桩 | 零调用 | spec@480 |
| PUT | /api/v1/system/hostname | api_v1.py:480(spec) | 纯桩 | 零调用 | spec@480 |
| GET | /api/v1/presets | api_v1.py:483(spec) | 纯桩 | 零调用 | spec@483 |
| POST | /api/v1/presets | api_v1.py:483(spec) | 纯桩 | 零调用 | spec@483 |
| POST | /api/v1/presets/load | api_v1.py:485(spec) | 纯桩 | 零调用 | spec@485 |
| POST | /api/v1/presets/delete | api_v1.py:487(spec) | 纯桩 | 零调用 | spec@487 |
| GET | /api/v1/presets/<name> | api_v1.py:489(spec) | 纯桩 | 零调用 | spec@489 |
| PUT | /api/v1/presets/<name> | api_v1.py:489(spec) | 纯桩 | 零调用 | spec@489 |
| DELETE | /api/v1/presets/<name> | api_v1.py:489(spec) | 纯桩 | 零调用 | spec@489 |
| GET | /api/v1/hardware | api_v1.py:492(spec) | 纯桩 | 零调用 | spec@492 |
| GET | /api/v1/hardware/display | api_v1.py:494(spec) | 纯桩 | 零调用 | spec@494 |
| PUT | /api/v1/hardware/display | api_v1.py:494(spec) | 纯桩 | 零调用 | spec@494 |
| GET | /api/v1/hardware/mouse | api_v1.py:496(spec) | 纯桩 | 零调用 | spec@496 |
| PUT | /api/v1/hardware/mouse | api_v1.py:496(spec) | 纯桩 | 零调用 | spec@496 |
| GET | /api/v1/network/wifi | api_v1.py:499(spec) | 纯桩 | 零调用 | spec@499 |
| POST | /api/v1/network/wifi/scan | api_v1.py:501(spec) | 纯桩 | 零调用 | spec@501 |
| POST | /api/v1/network/wifi/connect | api_v1.py:503(spec) | 纯桩 | 零调用 | spec@503 |
| POST | /api/v1/network/wifi/ap/apply | api_v1.py:505(spec) | 纯桩 | 零调用 | spec@505 |
| POST | /api/v1/network/wifi/client/activate | api_v1.py:507(spec) | 纯桩 | 零调用 | spec@507 |
| GET | /api/v1/license | api_v1.py:510(spec) | 纯桩 | 零调用 | spec@510 |
| GET | /api/v1/devices | api_v1.py:513(spec) | 纯桩 | 零调用 | spec@513 |
| GET | /api/v1/devices/<path:sub> | api_v1.py:515(spec) | 纯桩 | 零调用 | spec@515 |
| GET | /api/v1/makcu/devices | api_v1.py:517(spec) | 纯桩 | 零调用 | spec@517 |
| GET | /api/v1/ferrum/devices | api_v1.py:519(spec) | 纯桩 | 零调用 | spec@519 |
| GET | /api/v1/kmboxb/devices | api_v1.py:521(spec) | 纯桩 | 零调用 | spec@521 |
| GET | /api/v1/diagnostics | api_v1.py:524(spec) | 纯桩 | 零调用 | spec@524 |
| GET | /api/v1/diagnostics/<path:sub> | api_v1.py:526(spec) | 纯桩 | 零调用 | spec@526 |
| POST | /api/v1/diagnostics/<path:sub> | api_v1.py:526(spec) | 纯桩 | 零调用 | spec@526 |

### 设计器（4）
| 方法 | 完整路径 | 文件:行号 | 实现性质 | 前端调用方 | 备注 |
|---|---|---|---|---|---|
| GET | /api/v1/designer/css | api_v1.py:683 | 真实 | designer.js:9 | /api/v1 唯一有消费方的路由；/designer 页默认关（TTBOX_ENABLE_DESIGNER） |
| PUT | /api/v1/designer/css | api_v1.py:694 | 真实 | designer.js:9 | 同上 |
| DELETE | /api/v1/designer/css | api_v1.py:714 | 真实 | designer.js:9 | 同上 |
| GET | /ui-custom.css | api_v1.py:743 | 死端点 | 零调用 | grep 'ui-custom' → 0；api_v1.py:738 注释"index.html 仍在引用它"已过时（零外联锁） |

（另：GET /designer @api_v1.py:758 仅在 TTBOX_ENABLE_DESIGNER=1 时注册，默认不存在。）

## 四、同一功能多个端点（重复对）

| 重复对 | 性质 | 前端实际使用 |
|---|---|---|
| POST /api/ota/install(1970) vs POST /api/update/install(2018) | 逐字重复：1970-1996 与 _ota_install_impl()(2000-2015) 函数体完全相同 | update/install(12824)；ota/install 零调用 |
| POST /api/system/reactivate(1948) vs POST /api/system/master-reactivate(1960) | 逐字重复（同 409 下沉 T2.x） | reactivate(15885)；master-reactivate 零调用 |
| GET /api/config(2126) vs GET /api/v1/config(api_v1.py:225) | 同功能双代 | 两者均零调用（前端读 /api/state） |
| PUT /api/config(2097) vs PUT /api/v1/config(api_v1.py:242) | 同功能双代 | 前者(11149) |
| GET /api/models(2435) vs GET /api/v1/models(api_v1.py:294) | 同功能双代 | 前者 |
| POST /api/models/select(2773) vs POST /api/v1/models/activate(api_v1.py:300) | 同功能双代 | 前者(14691) |
| POST /api/models/delete(2761) vs POST /api/v1/models/remove(api_v1.py:327) | 同功能双代 | 前者(14777) |
| POST /api/models/rknn-concurrency(2855) vs POST /api/v1/models/set-concurrency(api_v1.py:336) | 同功能双代 | 两者均零调用 |
| POST /api/models/import(2703) vs POST /api/v1/models/import(api_v1.py:355) | 同功能双代（multipart vs src_path 两种传输） | 前者(15433) |
| GET /api/preview.jpg(4890) vs GET /api/v1/preview.jpg(api_v1.py:387) | 同功能双代 | 两者均零调用（前端只用 mjpg） |
| GET /api/preview.mjpg(4915) vs GET /api/v1/preview.mjpg(api_v1.py:420) | 同功能双代 | 前者(11254) |
| POST /api/control/start|stop(3593,3610) vs POST /api/v1/runtime/<action>(api_v1.py:286) | 同功能双代 | 前者(15057) |
| GET /api/model/list(framework_api.py:173) + GET /api/model/active(:191) vs GET /api/models(ttbox-web.py:2435) | 同功能双代（framework_api 旧命名） | 后者 |
| GET /api/wifi/status、GET /api/network/status(framework_api.py:132) vs GET /api/network/wifi(ttbox-web.py:4209) | 同功能双代 | 后者(12154) |
| GET /api/system/status(framework_api.py:132) vs GET /api/system(ttbox-web.py:1768) | 近似功能（不同路径，非路由冲突） | 都不调用 /api/system/status；/api/system 有调(12264) |
| /api/v1 的 28 条 NOT_IMPLEMENTED 桩 vs ttbox-web.py 同名真实端点 | 纯占位重复（v1 层未接线） | 全部零调用 |
| /api/plugins(framework_api.py:49) vs /api/plugins/market(framework_api.py:112) | 非重复：插件列表 vs 本地市场仓库 | 均零调用 |

## 五、定义了但无人引用的路径常量（plugins/web/lib/paths.py，107 行）

| 常量/函数 | 行号 | 引用情况 | 判定 |
|---|---|---|---|
| MOUSE_EVENT_SOCK_DEFAULT | paths.py:22 | 生产代码零引用；仅 core/tests/usbproxy_buttontest.py:8 引用（跨语言同值门禁断言用，ttbox_conventions_gate.sh:364） | 仅测试/门禁引用 |
| repo_root() | paths.py:37 | 无外部直接调用；仅被 scripts_dir()(:55) 内部使用 | 内部依赖，非死代码 |
| MOUSE_CMD_SOCK_DEFAULT | paths.py:21 | core/tests/usbproxy_state.py:4、usbproxy_ping.py:4、usbproxy_buttontest.py:8 + core/tools/usb_diag.py:5 | 正常（测试/工具引用） |
| WEB_PORT_DEFAULT | paths.py:26 | ttbox-web.py:77、framework_api.py:57、wifi_manager.py:27(同值副本)、验收脚本 | 正常（同值副本由门禁断言，非漂移） |
| IPC_SOCKET_DEFAULT / ttbox_prefix / scripts_dir / models_root / config_dir / presets_dir / web_credentials_file / hdmirx_edid_tool / motion_profiles_dir / ipc_socket | :20,32,46,58,68,73,78,90,97,104 | 均有生产引用（ttbox-web.py、api_v1.py、framework_api.py、lib/ipc.py） | 正常 |

结论：paths.py 无完全死常量；最接近死的是 MOUSE_EVENT_SOCK_DEFAULT（仅测试+门禁）。

## 六、取证方法说明（可复核）

1. index.html 全量 API 调用点：grep 模式 `['"\`]/api/[A-Za-z0-9_./${}?=&-]*` → 79 处（行号已列入上表"前端调用方"列）。
2. 零调用判定：对每个端点名做 alternation grep，关键词包括 import-onnx / convert-status / motion-profile / motion-training / /api/themes / theme-assets / /api/remote / auto-start / calibration / /api/events / kmboxb / master-reactivate / system/version / preview.jpg / game-profile / rknn-concurrency / hailo-pipeline / remote-frame / cloud-encrypted / health/frontend / branding / /api/plugins / core/status / model/list / model/active / system/status / network/status / wifi/status / fan/status / monitor/status / upgrade/status / hwmon / mouse/timing / full-recovery / reset-local / network/prepare / /api/v1 / ui-custom —— 全部 0 命中（唯一例外：'calibration' 命中 5783 的 HTML label 文本 `modelCalibrationZip`，非 API 调用）。
3. 非 fetch 调用形式专项复核（grep window.open / location.href / location.assign / location.replace / formAction / action=" / EventSource / .src= / <img / <video / <source / download=）→ 4 处命中：#1 `/`（11649 跳根页）、#4 `/activate`（7479+7490）、#50 presets/export（13503 `<a href target=_blank>`）、#112 preview.mjpg（11254 赋 preview.src）。无 form action、无 EventSource、无 window.open 指向 API。
4. 动态拼接核查：index.html 仅 `/api/presets/${name}/export`(8815) 与 `/api/system/storage${...}`(11454) 两处模板串拼接，无 `"/api/" + x` 形式。
