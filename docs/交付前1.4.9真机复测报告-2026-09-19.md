# 交付前 1.4.9 真机复测报告（2026-09-19）

**结论**：本轮把「OTA 正式地址接线 + 在线激活掉授权根因 + license 服务托管」三件事做完，
**在 RK3588 真机上跑通了「检查更新 → 下载 → 验签 → 安装 → 切版本 → 服务重启 → 授权自动恢复」全链**，
并确认 P0 的发布树属主问题在 1.4.9 包上不再出现。

仍**未达交付条件**：S1（板上残留与 core 内嵌公钥配对的授权私钥）卡在业主确认出厂镜像模板，
这是本轮唯一剩下的阻塞项。

| 面 | 结果 |
|---|---|
| 本地六条门禁 | 全绿 |
| ARM64 交叉编译 | BUILD_RC=0，aarch64 ELF，RUNPATH `$ORIGIN/../lib` |
| OTA 包 | 145 文件 / 3.1 MB / 7 道断言全过 / 真钥签名 / 篡改必败 |
| 服务器发布 | `signature_ok=true`，`/latest` 返回 1.4.9 |
| 板端 OTA | SUCCESS，`current → releases/1.4.9`，sha256 逐字节一致 |
| 板端装机核对 | 非 root 文件 **0**、禁入物 **0**、三处版本同值 1.4.9 |
| 在线激活（HTTPS） | `activated=true` / `state=active` / `valid=true` / 能力四项含 `ota` 全 true |
| 心跳稳定性 | 装后 6 分钟 + 重启后 5 分钟，会话每 60 秒被刷新，零错误 |
| 服务重启存活 | 重启 `ttbox-core` + `ttbox-web` 后 90 秒内自动恢复在线，未掉授权 |
| 板端 doctor | 全部通过（含两条 OTA 地址检查） |

---

## 一、产物标识（可逐项复算）

| 项 | 值 |
|---|---|
| 仓库提交 | `2731153`（批 2：OTA 正式地址 + 心跳 403 分层 + 版本升 1.4.9） |
| 包内 `manifest.git_sha` | `27311531a16f621aa0d510b2d5ac40740318829b` |
| 核心版本真源 | `core/CMakeLists.txt:14` = `1.4.9`（`version.hpp` 同值） |
| 交叉编译产物 sha256 | `9adf533986eaab961f437c0562f371c547b911e61ea3a6e01a8cf0236e3682cd` |
| OTA 包 sha256 | `f7adfe1cf9f708e3016041a28dcc3fa55c36ecfe1557f058e9824525a8b95399` |
| 包文件数 | 145（`RELEASE_MANIFEST.json` 内 `files_sha256` 条数 = 145，与 tar 内普通文件数相等） |
| 签名 key_id | `ttbox-ota-2026b` |
| 发布地址 | `https://cctv2.top:10046/ota/pkgs/ttbox-update-1.4.9.tgz` |

---

## 二、本地门禁（六条，全绿）

| 项 | 命令 | 结果 |
|---|---|---|
| 文档链接 | `python docs/check_links.py` | `broken_count=0`，RESULT=PASS |
| 口径门禁九项 | `bash scripts/ttbox_conventions_gate.sh` | ①–⑨ 全 OK，`RESULT: PASS`；⑥ 版本同值 core 1.4.9 |
| Core 单测 | `cd /c/ttbox-local/core && ctest --test-dir build-j --output-on-failure` | **29/29 passed**（连跑两次确认） |
| 平台/框架单测 | `python -m pytest --import-mode=importlib -q platform/tests framework/tests` | 92 passed |
| 面板单测 | `cd plugins/web && python -m pytest tests -q` | **273 passed**（含本轮新增 5 条心跳用例） |
| OTA 验签 | `python tools/ota/test_verify.py` | 0 failures |

★ **一处已查清的偶发抖动**：首次全量 ctest 出现 `test_hotkey_guard` 失败，
单独跑通过（2.03s）、随后连跑两次全量均 29/29 通过。
根因在用例自身——`core/tests/test_hotkey_guard.cpp:127` 用墙钟
`sleep_for(30ms)` 逐周期推进再读 `thread.status()`，机器同时跑 WSL 编译/多批 pytest 时
采样会落后。**不是产品回归**，但它意味着这条用例在负载下不可靠，建议后续改成
「轮询到状态变化或超时」而不是固定 sleep。

---

## 三、交叉编译与出包

```bash
# 交叉编译（WSL，源 /mnt/c/ttbox-local）
cmake -B build-aarch64-t114 … && cmake --build build-aarch64-t114 -j8
#   BUILD_RC=0；build-aarch64-t114/ttbox_core_main: ELF 64-bit LSB pie, ARM aarch64
#   RUNPATH: [$ORIGIN/../lib]
#   NEEDED 10 项全部在发布树 lib/ 或系统 lib 内可解析

# 出包 + 签名（唯一入口，白名单单一真源 deploy/pack_manifest.txt）
bash scripts/ttbox_pack_ota.sh build-aarch64-t114 dist \
  --priv /mnt/c/ttbox-ota-keys/ttbox-ota-2026b.priv.pem \
  --password-file /mnt/c/ttbox-ota-keys/PASSPHRASE.txt \
  --key-id ttbox-ota-2026b
```

断言输出：断言 0 无 world-writable / 断言 1 顶层闭集 `bin deploy framework lib plugins scripts ttbox_motion usbproxy` /
断言 2 无密钥无垃圾无构建残渣 / 断言 3 `bin/` 恰 1 文件 / 断言 4 双向核对无混入无漏装 /
断言 5 版本号 1.4.9 / 断言 6 tar 布局 `RELEASE_MANIFEST.json` + `payload/` 无 `./` 前缀。

**独立验签（13 项，用板端同一个 `OtaUpdater._verify_signature()`）**：全 PASS。要点：

- `sign.json.sha256` == 包实测 sha256；
- 板端同函数验签通过；`key_id` 不符 → 拒绝；`sha256` 被篡改 → 签名失效；
- 包内 145 个文件逐条 sha256 与 `RELEASE_MANIFEST.json` 一致；
- 包内 `bin/ttbox_core_main` == 本地交叉编译产物（同 sha256）；
- 无 `docs/ tools/ platform/ modules/ tests/`，无游离私钥。

---

## 四、服务器发布

桥接层管理路由只监听 `127.0.0.1:3100`，外网 `/api/` 走的是 license_system，
所以发包必须在服务器本机做：

```bash
# ① Windows → 服务器（plink 灌字节流；服务器回显 sha256 与本地逐字符相同）
plink -ssh -P 10015 … "cat > /tmp/ttbox-1.4.9.ota.tgz; sha256sum /tmp/ttbox-1.4.9.ota.tgz" \
  < ttbox-1.4.9.ota.tgz
# ② 服务器本机发布
curl -sS -X POST -H "X-Admin-Token: <BRIDGE_ADMIN_TOKEN>" \
  --data-binary @/tmp/ttbox-1.4.9.ota.tgz \
  "http://127.0.0.1:3100/api/ota/publish?version=1.4.9&publish=1"
```

返回 `{"ok":true,"version":"1.4.9","bytes":3219806,"sha256":"f7adfe1c…","key_id":"ttbox-ota-2026b","published":true}`。
服务器用自己的私钥重签旁车（`key_id` 相同、`sha256` 与我们本地一致，`signature` 因 `built_at` 不同而不同属正常），
自校验 `signature_ok=true`。外网 `GET /ota/latest?current=1.4.8` 返回 1.4.9 的
`package_url` / `sign_url`（两者皆 https，且 `sign_url == package_url + ".sign.json"`）。

---

## 五、板端 OTA（真实链路，时间线）

从面板同一入口触发的（`POST /api/update/install`，写任务文件 → `ttbox-ota.path` 拉起 root 更新器）：

| 时刻（板端 UTC） | 事件 |
|---|---|
| 13:21:54 | 装前基线：`current=/opt/ttbox/releases/1.4.8`，面板 `app_version=1.4.8` |
| 13:22:01 | 面板 `/api/update/check` 返回 200 + `update_available=true`、`latest_version=1.4.9`（**原先这里 503**） |
| 13:22:01 | 下发安装，返回 `{"ok":true,"data":{"message":"更新任务已受理","scheduled":"job-1789824121176.json"}}` |
| 13:22:09–13:27:10 | `ota_status.json` 持续 `RUNNING`（服务器→板下载 ~13 KB/s，3.1 MB 约 5 分钟，**不是卡死**） |
| 13:27:10 | `current` 已切到 `/opt/ttbox/releases/1.4.9`，core 以新二进制重启 |
| 13:27:25 | `ota_status.json` = `{"state":"SUCCESS","version":"1.4.9","sha256":"f7adfe1c…"}` |

**装机核对**：

- `current → /opt/ttbox/releases/1.4.9`，`readlink -f` 确认；
- **`find /opt/ttbox/releases/1.4.9 -not -user root | wc -l` = 0** —— 1.4.7 曾出现 61 个 ttbox 属主文件（staging 期可篡改），本条已归零；
- 树内相对包多出 3 个文件：`RELEASE_MANIFEST.json`（包根，不入 payload）+ 2 个运行时生成的
  `plugins/web/lib/__pycache__/*.pyc`，属正常；
- `docs/ tools/ platform/ modules/ tests/` 各 0 个文件；
- 面板 `/api/state.app_version` = `1.4.9`，`RELEASE_MANIFEST.json.version` = `1.4.9`，
  `current` 目录名 = `1.4.9` —— **三处同值**（S7 收口）；
- `1.4.8` 树保留作回滚。

---

## 六、在线激活 over HTTPS（本轮核心）

**板端配置**（`/opt/ttbox/config/default.json`，属主权限保持 root:ttbox 0640）：

```json
"license_base_url": "https://cctv2.top:10046/ttbox",
"app_secret":       "bc4ec67b…4ede"（与服务器 bridge/app_secret 一致）
```

**激活证据**（`GET /api/license` 原始字段）：

```json
"license": {
  "activated": true, "state": "active", "status": "active", "valid": true,
  "plan": "subscription", "expires_at": "2026-10-19T12:54:25Z",
  "device_id": "opi-9ecf266dc8154491",
  "capabilities": {"aim": true, "capture": true, "inference": true, "ota": true}
},
"cloud": {
  "card_mask": "B9TV-****HGBP", "source": "cloud",
  "machine_code": "9ecf266dc8154491", "max_devices": 1,
  "heartbeat": {"online": true, "interval": 60, "timeout": 180, "error": "", "last_ok_at": 1789825220.5}
}
```

四项能力（含 `ota`）全 true 是本轮关键——`/api/update/install` 有 `capabilities.ota` 门控，
能走通 OTA 本身就证明在线授权链是活的。

**心跳稳定性**（板端 UTC，每 30 秒采一次会话文件 mtime）：

- 装后阶段：13:28:10 → 13:33:41，12 次采样，`session_age` 在 18–58 秒之间循环，
  说明会话每 60 秒被刷一次，`heartbeat.error` 恒为空；
- 重启阶段：13:34:11 重启 `ttbox-core` + `ttbox-web`，90 秒后两服务 active；
- 重启后阶段：13:35:42 → 13:40:13，10 次采样，行为与重启前完全一致，`expire_at` / `card_mask` 不变。

**结论：服务重启后授权自动恢复，没有掉回 restricted。** 这正是本轮修掉的缺陷（见第八节）。

---

## 七、板端 doctor

```
[doctor][PASS] current -> /opt/ttbox/releases/1.4.9（版本 1.4.9）
[doctor][PASS] ttbox-core.service active
[doctor][PASS] ttbox-web.service active
[doctor][PASS] ttbox-usbproxy.service active
[doctor][PASS] ttbox-preview.service active
[doctor][PASS] ttbox-edid.service active
[doctor][PASS] /dev/video0 存在（HDMI 采集节点）
[doctor][PASS] boot 配置含 overlay 配置（人工确认 hdmirx overlay 已生效）
[doctor][PASS] python3-cryptography 可用
[doctor][PASS] OTA 公钥在位（ttbox-ota-2026b）
[doctor][PASS] OTA 更新器在位
[doctor][PASS] OTA 任务目录在位（root:ttbox 0770）
[doctor][PASS] OTA 服务器地址两处一致（https://cctv2.top:10046/ota）
[doctor][PASS] OTA 服务器地址非占位值
[doctor][PASS] ttbox-ensure.timer active（自愈巡检在跑）
[doctor] 全部通过 ✓
```

原先 doctor 那条红（占位地址）已消失，**16/16 全绿**。

---

## 八、本轮修掉的缺陷

### S8 在线激活 1 分钟内掉授权（P0）

**根因是时区错配**，不是 403 文案问题：桥接层按 UTC 写 `online_sessions.lastHeartbeat`，
`license_system` 的 Sequelize 配 `timezone:'+08:00'` 去读，每条活跃会话凭空老 8 小时；
`license/api/src/scheduledTasks.js` 每分钟跑 `checkTimeout()`，超阈值一律置 `isValid=0`
⇒ 盒子下次心跳收 403 ⇒ core 掉回 restricted。实测会话 `id=23` 被读成
`2026-09-19T05:02:37Z`（差 8h），算出 age 29128s ≫ 180s 判超时。

修了四层：

| 层 | 改动 |
|---|---|
| 根因 | 服务端 `license/api/src/config/database.js`：`timezone:'+08:00'` → `'+00:00'` |
| 阈值 | 服务端 `license/api/.env`：`HEARTBEAT_TIMEOUT=30` → `180` |
| 契约 | 服务端 `bridge/src/license.js`：403 拆两句（强制下线 vs 登录状态已失效） |
| 盒子自愈 | `plugins/web/lib/heartbeat_worker.py`：403 先判会话丢失 → relogin 自愈（上限 3 次）；强制下线/卡密到期仍锁 core |

盒子侧改动随 1.4.9 上板，新增 5 条单测在旧逻辑上全红、恢复后全绿（反向验证）。
服务端改动即时生效，板上复测见第六节。

### S9 license 后端不是 systemd 托管（运维隐患）

原先 `node src/app.js` 是手工起的孤儿进程，`systemctl` 里查不到，崩了或关机重启不自拉起，
表现是「面板和 `/api/` 一起挂」。新增 `/etc/systemd/system/ttbox-license.service`
（`Restart=on-failure`，`.env` 由 dotenv 自动加载）并 `enable --now`。
现役 `ttbox-bridge` / `ttbox-license` / `nginx` 三个单元全 active。

---

## 九、遗留问题

1. **S1（唯一阻塞项，等业主）**：板上 `/root/ttbox-src/tools/license/.testkeys/` 存着与
   core 内嵌公钥**配对**的授权私钥，拿到设备即可自签任意授权卡；同目录还有已作废的 OTA 2026a 私钥。
   `/root/ttbox-src` 是 88 MB 源码副本，属主 uid 197108（Windows scp 残留）。
   需业主确认**出厂镜像是否以本板为模板**——若是，必须先清残留并按 S1-A 轮换密钥。
2. **测试卡 `B9TV-TLNN-E19Y-HGBP`**：本轮为验证在线激活新发，绑在 `opi-9ecf266dc8154491`，
   有效期至 2026-10-19。**当前保留着**，因为删了板子就掉回未授权、没法演示；
   需要清理时在面板或 DB 删 `licenses` 对应行即可。
3. **口径观察项**：`/api/license` 里的 `app_version` 是 `2026.08.03.1`、`core.version` 是 `2026.05.16`，
   与 `/api/state.app_version`（1.4.9）不是一个东西，但同屏出现容易让人误判成 S7 复发。
   建议前端加标签区分，或在文档里写清各自含义。
4. **证书续期**：Let's Encrypt YE1，**2026-12-18 到期**，手动 DNS-01 两阶段，到期前必须人工重跑。
5. **`test_hotkey_guard` 负载下偶发红**（见第二节），建议改为轮询等待。
6. **慢链路**：服务器→外网下载实测约 13 KB/s，3 MB 包要 5 分钟。板端 OTA 会长时间显示
   `RUNNING`，验收时别当卡死；长期看值得让业主升带宽或换分发方式。

---

## 十、复现命令

```bash
# 板端连接（ssh 在 22）
plink -ssh -pw root -hostkey SHA256:JCSZToi0WY02mohoJm6g7ap5ii6fWP1Dzy1lVjozvq0 \
  -batch root@192.168.0.104

# 板端「检查更新」（200 = 正常；返回 update_available / latest_version）
curl -sS -X POST http://127.0.0.1:8000/api/update/check

# 板端「安装」（同一入口，写任务文件触发 root 更新器）
curl -sS -X POST -H "Content-Type: application/json" \
  -d '{"url":"https://cctv2.top:10046/ota/pkgs/ttbox-update-1.4.9.tgz","key_id":"ttbox-ota-2026b","version":"1.4.9"}' \
  http://127.0.0.1:8000/api/update/install

# 进度与结果
cat /opt/ttbox/state/ota_status.json
readlink -f /opt/ttbox/current

# 授权/心跳
curl -sS http://127.0.0.1:8000/api/license
stat -c '%y %n' /opt/ttbox/config/cloud_session.json

# 装机安全核对（应为 0）
find /opt/ttbox/releases/1.4.9 -not -user root | wc -l

# 运维健康
/opt/ttbox/current/scripts/ttbox.sh doctor
```
