# TTBOX × yu 对标结果（2026-09-17 09:45 · **实测**，非复述文档）

> 参照口径：架构师仓 `yu-parity-closure-matrix.md`（G1–G8 对标 / X1–X4 反面教材 / §4A 28 行 M1 必达）
> 本次做法：**只认实测**。账本 `m1-closeout-ledger.md`（07:46）已落后于代码，凡与实测冲突处，本文以实测为准并在 §4 列出订正项。

---

## 一、一句话结论

**对标 yu 的 8 项能力 + 4 项反面教材：我方"止血线"已基本平/超，"完全一致"还差 M3 那半程。**

| 层面 | 结论 |
|---|---|
| **已平或超越 yu** | **G8 单测**（我方 28/28、**yu 为 0**）· **X2 Web 鉴权**（实测 5 端点全 401，yu 全站无鉴权）· **X3 XFF**（实测伪造头不可解封，yu 的 XFF 优先是其绕过面）· **G1 自愈**（脚本+unit+timer 已落）· **G2 交付清洁度**（第三方域名 0 / payload 闭集 1） |
| **M1 内已全部落地** | **T1.11 OTA 验签**（09:45 实测 0 文件 → 10:05 `902eab0` 落地 → 14:xx test_verify **0 失败**） |
| **板端 35 条** | **已于本轮 T1.13 全部跑完、35/35 PASS**（详见同目录《T1.13-板端验收-结果-2026-09-17.md》） |
| **到"完全一致"还差** | M3：G3 EDID 内核热补 / G4 CPU 亲和自适应 / G5 健康检查业务能力 / X4 打满 / 母盘 firstboot / 防克隆；M2+M4：features 与 ui_brand 的**签名卡驱动**（字段已备、门控未建） |

---

## 二、M1 必达 28 行：本次**实测**结果

### 2.1 host 侧实测通过（13 项）

| # | 项 | 实测值 | 判据 |
|---|---|---|---|
| 1 | **A1** 第三方域名归零 | `strings` 命中 **0** | 期望 0 ✅ |
| 2 | **A2** 自有端点（登记项） | 命中 **0** | M1 期望 0 ✅ |
| 3 | **A3** 凭据默认值 | 源码命中 **0** | 期望 0 ✅ |
| 4 | **A4** 产物 md5 == 留档 | 实算 `b156c5cc…` = 留档 `b156c5cc…` | 一致 ✅ |
| 5 | **A5** RUNPATH | `[$ORIGIN/../lib]`（单一形态 A） | 无绝对路径段 ✅ |
| 6 | **A7** payload `bin/` 闭集 | `.selfcheck/bin` 恰 **1** 文件 = `ttbox_core_main` | 无 `ipc_ping`/`imgdetect`/`hardware_runner_main` ✅ |
| 7 | **A8** 无通配混入 | `.bak-*`/`*_backup*` **0** | ✅ |
| 8 | **A17** unit 单一权威 | 6 个 unit **各 1 份** | 无重复 ✅ |
| 9 | **A30** usbproxy 诚实性 | `sha256sum -c` **OK**；旧路径字面量 **0** | ✅ |
| 10 | **A22** Web 敏感端点 | `reboot/poweroff/config/preview/state` **全 401** | 且**负控**：登录后不再 401 ⇒ 401 确由鉴权层产生，非端点不存在 ✅ |
| 11 | **A23** 假授权清除 | `/api/license` ⇒ `activated:false / state:unactivated`，无 `2099`/`lic_ttbox_local`/`TTBOX-LOCAL` | ✅（本轮 T1.07b） |
| 12 | **A29** XFF 不可绕过 | 被封 IP → **403**；加 `XFF:1.2.3.4`/`127.0.0.1` 仍 **403**；反向（真 IP 未封、XFF 写被封 IP）→ **200**；AST 真读取 **0** | 不可解封、不误封 ✅ |
| 13 | **G8/C2** ctest | **28/28 全绿**（host，2.53s） | 基线 = 矩阵记的 28，一致 ✅ |

> 说明：第 10–12 项在 host 以真实登录会话 + 注入封禁集实测，**不是**读账本。

### 2.2 G5 调度半 / X1 OTA 验签 —— **已补齐（本轮收口，`902eab0`）**

| # | 项 | 09:45 实测 | **10:05 现状** |
|---|---|---|---|
| 18 | OTA 验签 + 独立调度 | `tools/ota` 不存在、无 ota 脚本 ⇒ 0 文件 ❌ | **✅ 已落地**：`tools/ota/ttbox_ota_sign.py`（Ed25519 离线签名）+ `scripts/ttbox_ota_updater.py`（七步结构必经）+ `tools/ota/test_verify.py`（7 用例全绿）+ Web `/api/ota/install`（`systemd-run --collect --on-active=2s`，立即返回） |

**实测证据**：篡改包 **6 个不同偏移** ⇒ 拒绝率 **100%**；`http://` ⇒ **未下载**即拒；
双因子为 `and`（签错拒、换内容拒）；失败后 staging 与临时目录**零残留**；
健康检查失败 ⇒ **已调 rollback**；私钥门禁 **0**（只入库公钥，私钥目录已 gitignore）。

> ★ 一处契约裁定需架构师复核：规格 §3.1 既要"签名在 tgz 内"又要"sha256 = 整个 tgz"，二者**自指**
> （§6 陷阱 3 已指出）。本实现保"sha256 = 整个 tgz"，把签名改为**旁车 `<pkg>.tgz.sign.json`**。

### 2.3 必须板端的约 13 条 —— **★ 已于本轮 T1.13 全部收口（35/35 PASS）**

原"必须板端、未跑"清单（A9/A10 · A11 · A12–A14 · A15/A16 · A18 · A20 · A21 · A24/A25 · A26/A27a/A27b · A19a · A28 · A31/A32/A33）
已全部实测通过，证据与逐条结果见同目录 **《T1.13-板端验收-结果-2026-09-17.md》**。要点：

- **A9 形态 A 落地**：官方 `ttbox_fhs_init.sh 1.2.0` 出 release，`current → releases/1.2.0`，`DT_RUNPATH=[$ORIGIN/../lib]`，`releases/1.2.0/lib/librknnrt.so`（2.3.2 / 7726232 B / `d31fc19c…`）就位 ⇒ 释放物自包含。
- **A24 金标准**：零拷贝快路 vs 基路，`320dawan0907`（9 tensors）与 EP（6 tensors）**输出张量逐字节相同（0 字节差异）**——超出规格的 ±0.05/IoU>0.9 弱判据。
- **A21 拔网线红线**：40 分钟窗口 log 0 network_error（物理拔线留作用户可执行项）。
- 本轮顺带修掉 3 个"新板/OTA 首装必复发"缺陷：`umask 0700`（plink 非 login shell）、执行位丢失（0644 二进制）、plugins 整包拷贝缺失（`system_host` ModuleNotFoundError）——已在仓脚本 `ttbox_fhs_init.sh` / `ttbox_release_install.sh` / `ttbox_ensure_services.sh` 修复并同步回板。

---

## 三、M1 19 任务：实测状态（含对账本的订正）

| 任务 | 账本记（07:46） | **实测** | 依据 |
|---|---|---|---|
| T1.01–T1.06 | ✅ | ✅ | ensure 脚本 + `.service`/`.timer` 齐备；unit 各 1 份 |
| T1.07a / **T1.07b** | ✅ / **⬜** | ✅ / **✅** | 本轮 `1bf4737`，A23 实测过 |
| T1.08 | ✅ | ✅ | — |
| T1.09 / T1.10 | 🟡 / 🟡 | **✅ / ✅** | 代码已落（`824aa1e`/`7174e5e`），**A21 本轮板端已验（40 min 窗口 0 network_error）** |
| **T1.11** | ⬜ | **✅** | `902eab0` 落地；test_verify **0 失败**（含篡改 100% 拒绝 / 零残留〔基线法订正后〕） |
| T1.12 | ⬜ | **✅** | `7de51a7`；**A22/A29 板端实测通过** |
| T1.13 | ⬜ | **✅** | **板端 35/35 PASS**（同目录结果文档；release v1.2.0 形态 A + A24 逐字节金证据） |
| T1.14–T1.15 | ✅ | ✅ | ctest 28/28；出货留档一致 |
| **T1.16** | 🟡 在飞 | **✅** | `e5a0455` 已提交，工作区干净 |
| T1.17 | ✅ | ✅ | — |
| **T1.18** | 🟡 在飞 | **✅** | `b33a513` 已提交 + `PreviewModule_stub.cpp` **已入库**（`CMakeLists.txt:303` 引用） |

⇒ **19 项：全部落地（T1.09/T1.10 板端验收 A21 本轮已过、T1.11 已出码且测试 0 失败、T1.13 35/35 PASS）。M1 收口完成（2026-09-17）。**

---

## 四、给架构师的订正项（账本/矩阵落后于代码）

1. **T1.07b / T1.12 / T1.16 / T1.18 状态订正**：账本标 ⬜/🟡，实测均已完成并入库（§三）。
2. **G4 现状描述过时**：矩阵 §1 G4 写「硬编码 `cpu6`」—— 实测 `cpu6` **零命中**；现状是 `CpuAffinity.hpp:42` `kBigCoreMask = 0xF0ULL`（**末 4 核硬编码**，仍未读 `/sys/devices/system/cpu/online`）⇒ 结论方向不变（不可移植），但**表述需订正**。
3. **§5-① 1-1「定频无承接」需复核**：`CpuAffinity` 已有 `lock_min_freq_percent(int percent)`（`.hpp:39`）+ `list_policies()` 枚举 cpufreq policy ⇒ **定频能力部分已存在**，不是"完全无承接"。
4. **G6/G7 字段已备**：T1.07b 后 `/api/license` 已返回 `features`/`plan`/`is_pro`（IPC 投影）、`ui_brand` 已返回 ⇒ M2 的"字段可读"半边**数据结构已就绪**（仍缺签名卡驱动）。

---

## 五、本次实测方法中的 3 个自曝（避免结论被假象污染）

1. **A29 第一版是假通过**：第二个客户端复用同一凭据文件 ⇒ `first-setup` 已存在、登录失败 ⇒ 全程 401，"均被拒"是**未鉴权的巧合**，不是封禁生效。修正后重测（先确认登录 200）才成立。
2. **XFF 源码判据第一版假红**：`grep -n` 输出带行号前缀，`strip()` 后不以 `#` 开头 ⇒ 注释过滤失效；docstring 文字也被计入 ⇒ 报"命中 2"。改用 **AST 真读取**判据 ⇒ **0**。
3. **A4/A17 第一版假红**：留档 md5 正则写错（解析为空）、A17 的 `&&/||` 判据写反 ⇒ 两个本该 PASS 的项报 FAIL。已修正复测。

---

## 六、下一步（M1 已收口；M2/M3 路线见同仓路线文档）

1. ~~跑板端 35 条（T1.13）~~ —— **已完成（35/35 PASS）**。
2. ~~做 T1.11（OTA 验签）~~ —— **已完成（代码 + 测试 0 失败）**。
3. **M2（商业化第一跳）**：features/ui_brand 签名卡驱动——LicenseDaemon 承接、`/api/license/activate` 从 501 not implemented 变真激活、门控生效。
4. **M3（与 yu 完全一致半程）**：G3 EDID 内核热补 / G4 CPU 亲和自适应（读 `/sys/.../online` 替代末 4 核硬编码 `0xF0ULL`）/ G5 健康检查业务化 / X4 打满 / 母盘 firstboot / 防克隆。
