# 代码梳理 · 批次 0/1/4 —— 交付总结

- 日期：2026-09-17
- 团队：`software-ttbox-refactor`（主理人 齐活林 / 架构师 高见远 / 工程师 寇豆码 / QA 严过关）
- 依据方案：`docs/handover/2026-09-17/代码梳理方案-2026-09-17.md`（555 行）
- 提交锚：`7490668`（M2.07.1 基线）→ `ec6e66e`（批次 0/1/4）→ `ffcf095`（断链修复 + 命名归一）

---

## 1. TL;DR

**交付了什么**：在**零出货树改动**的前提下，完成代码梳理批次 0/1/4 —— docs 目录按「消费方」五层分类 + 中文目录命名归一（旧路径留指针桩）、本机 45MB 构建/探针产物归档（**归档非删除**，零 `rm`）、构建目录与发布约束常态化落文档。并附一次 `platform/` 板端只读实测，为其后续处置给出结论。

**状态**：全部完成，QA 独立验证 **IS_PASS: YES / 智能路由 NoOne**（无需返工）。

---

## 2. 交付概览

| 项 | 结果 |
|---|---|
| 交付状态 | ✅ 完成（批次 0/1/4） |
| 出货树改动 | **0 字节**（`core usbproxy scripts config deploy` diff 为空） |
| 板端核心 sha | `3f760bc1…`（1.4.3，未动） |
| git 提交 | 2 笔（`ec6e66e`、`ffcf095`） |
| 跟踪文件数 | 714（工作树干净，remote=0） |
| 断链检查 | `broken_count=0 RESULT=PASS` |
| QA 判定 | IS_PASS YES / NoOne |
| 已知遗留 | 3 条轻微文档口径差异 + 1 条沉淀缺陷 F-A（exec 位） |

---

## 3. 批次内容明细

### 批次 0 —— 文档分类与命名（commit `ec6e66e`）

按「消费方与生命周期」五层分类（源/部署/工具/文档/测试），**保持 12 个顶层目录不变**，只重排 `docs/` 内部：

| 新目录 | 归入内容 |
|---|---|
| `docs/protocols/` | `ipc-protocol.md`、`image-spec.md` |
| `docs/ops/` | `release-constraints.md`（新增，8 条硬约束 C1–C8）、`build-dirs.md`（新增）、`RK3588开发流程.md`、`测试说明.md`、`问题排查.md` |
| `docs/guide/` | 小白使用说明 + 小白教程 01–10 |
| `docs/verification/` | 真实 HDMI 闭环验证 |
| `docs/product/` | 项目路线图、TTBOX_WEB_FEATURE_MATRIX |
| `docs/architecture/` | 架构总览/模块说明/目录结构/插件系统/完整链路 |
| `docs/archive/` | 历史阶段文档（含 stages/performance） |

新增 `docs/CONVENTIONS.md`（129 行，路径规约）、`docs/废弃代码清单.md`（唯一死代码真相表）。

### 批次 1 —— 本机产物归档（同一 commit）

`.archive-2026-09-17/`（45MB，已 `.gitignore`）收纳：根目录 39 个 `_*` 探针、`build-aarch64-t114-m203-gate.log`、`build-aarch64/`、`build-aarch64-authon/`、`build-aarch64-t118/`、`core/build-host-m2/`、`models/`、`.pytest_cache` + 14 个 `__pycache__/`。

> **零 `rm`**：全部为 ARCHIVED 移动，可逆。`build-aarch64-t114/`（出货构建目录）与 `build-host-yu/`（host 测试目录）**原地保留**。

### 批次 4 —— 中文目录命名归一（`ec6e66e` + `ffcf095`）

`docs/架构/`→`architecture/`、`docs/开发/`→`ops/`、`docs/规划/`→`product/`、`docs/验证/`→`verification/`；**4 个旧路径各留一个 `README.md` 指针桩**（非重复文件，指向新位置），保证历史链接不硬断。

### 附：`platform/` 板端只读实测（同 commit）

结论：`platform/` 在**运行期结构上不可达**（无进程引用），**可删**，但**必须随 release 一起出货**——它与 `fhs_init.sh:382` 的 `tcopy platform`、`pyproject.toml:35` testpaths、`:32` packages.find、`RELEASE_MANIFEST.template.json:2` 注释、板端既有目录存在**6 个删除耦合点**。故**本批不动**，留待批次 2/3（发版窗口）。

---

## 4. 收货验证（主理人独立复核）

- `git diff --stat 7490668..ffcf095 -- core usbproxy scripts config deploy` → **空**：出货树零改动。
- `docs/check_links.py` → `broken_count=0 RESULT=PASS`（残留 12 条 `file:///g:/工作区/…` 为 `docs/archive/**` 历史绝对 URI，属**归档内容、不重写历史**，仅告警不计断链）。
- 4 个中文旧目录均为单文件 `README.md` 指针桩，无内容重复。
- 尖刀测试：向 docs 注入一条伪断链 → 检查器报 `broken_count=1 RESULT=FAIL`（证明检查器非橡皮图章）。

### QA 独立验证（3 项，IS_PASS YES / NoOne）

1. **platform/ 板端复测**：独立复现，结论一致；列出 6 个删除耦合点。
2. **3 份规约文档 vs 方案**：无缺漏/矛盾条款，8 条硬约束齐全，0 处含糊措辞；发现 3 条**轻微口径差异**（见下）。
3. **出货零改动 + 归档完整性**：`git diff` 空、2 份 `RELEASE_BUILD.md` 未变、核心 sha `3f760bc1…` 完好；`.archive-2026-09-17/build-dirs/build-aarch64/ttbox_core_main` = `e892e2f4…` ≠ `3f760bc1…`，证明**出货产物未被误归档**。

---

## 5. 已知遗留 / 待决

| 编号 | 级别 | 内容 | 建议 |
|---|---|---|---|
| **F-A** | 中 | **全仓 714 个跟踪文件 exec 位均为 0**（全 100644），含 `usbproxy/usb-proxy` 预编译 ELF。Linux/WSL 全新 clone 会得到不可执行脚本；当前出货靠工作树 exec 位 + `fhs_init` chmod 兜底。 | 属 **git 锚质量**问题、**非本批引入**。建议单独决策：`git update-index --chmod=+x` 逐个修 + WSL 跑 `scripts/ttbox_git_mode_check.sh` 固化。 |
| D-1 | 轻 | `build-dirs.md` 落在 `docs/ops/` 而非 `docs/build/`（方案文字未强制，属归类自由度）。 | 认可现状，无需动。 |
| D-2 | 轻 | 文档写 `HEAD=714` 文件、方案基准为 `704`（各时点真实值，非错误）。 | 以文档为准，不改。 |
| D-3 | 轻 | 中文目录改名落在**本批**（方案文本列在批次 2/3）。 | 提前做无副作用，认可。 |
| 批次 2/3 | — | platform/ 删除、static/legacy、根 `third_party`、`scripts/legacy`、测试根归一。 | **等发版窗口**（用户 Q3 明确「暂无，先只做 0/1/4」）。 |
| F12 | — | 离线卡接管后 web `cloud.source` 仍显示 'cloud'。 | 承自 M2.07.1，待排。 |
| KEEP_VERSIONS | — | `scripts/ttbox_release_install.sh:50` `KEEP_VERSIONS=2` 会静默清旧版，回滚深度仅 2。 | 承自 M2.07.1，建议评估调深。 |

---

## 6. 文件清单

**新增**
- `docs/CONVENTIONS.md`（129 行）
- `docs/ops/release-constraints.md`（56 行，8 条硬约束）
- `docs/ops/build-dirs.md`（94 行）
- `docs/check_links.py`（断链检查器）
- `docs/architecture/`、`docs/protocols/`、`docs/guide/`、`docs/verification/`、`docs/archive/`（目录）
- `docs/开发/README.md`、`docs/规划/README.md`、`docs/验证/README.md`、`docs/架构/README.md`（指针桩）
- `docs/handover/2026-09-17/代码梳理方案-2026-09-17.md`（方案，555 行）
- `.archive-2026-09-17/`（45MB，gitignored）

**移动/改名**
- `docs/ipc-protocol.md` → `docs/protocols/ipc-protocol.md`（拆分，根留精简版）
- `docs/开发/修改指南.md` → `docs/ops/修改指南.md`
- `docs/规划/项目路线图.md` → `docs/product/项目路线图.md`
- `docs/验证/真实HDMI闭环验证.md` → `docs/verification/真实HDMI闭环验证.md`
- `docs/架构/*` → `docs/architecture/*`
- 小白教程 01–10 → `docs/guide/小白教程/`

**修改**
- `.gitignore`（+`/.workbuddy/`、`/.archive-2026-09-17/`、`config/cloud*.json`）
- `modules/README.md`、`modules/01-capture/README.md`、`modules/04-decoder/README.md`、`docs/web/model/INDEX.md`、`docs/product/TTBOX_WEB_FEATURE_MATRIX.md`（链接随目录更新）

---

## 7. 用户下一步建议

1. **F-A exec 位**：单独开一条小任务修 git 索引 mode（`git update-index --chmod=+x`），并入库 WSL 校验脚本，避免"clone 即坏"。
2. **发版窗口到了再启批次 2/3**：删 `platform/` 前，先按 6 个耦合点逐条清理（`fhs_init.sh:382` 等），并跑一次全旅程验收。
3. 12 条 `file:///g:/…` 历史绝对 URI：若要清理，建议**只加注不改历史**（在 `docs/archive/` 加一条 README 说明来源机器路径）。
4. `KEEP_VERSIONS` 回滚深度评估：如商业出货需支持回滚到 1.4.x 之前的更早版本，需调大该值。
