# TTBOX 交付方案（2026-09-19）

目标：把当前「未达到交付条件」的状态推进到「可交付」，并给出每一步的核对方式与通过标准。
基线：仓库 HEAD（本机）+ 板端 192.168.0.104（Ubuntu / RK3588，current = 1.4.7）
依据：`.workbuddy/artifacts/交付条件检查-2026-09-19.md`（阻塞 5 条 + 待判 1 条）

---

## 一、什么叫「可以交付」

先定放行标准，再谈怎么改。十条全绿才放行，任一条不过不出包。

| # | 放行条件 | 核对方式 | 通过标准 |
|---|---|---|---|
| 1 | 设备上无任何签发私钥 | 板端 `find / -xdev \( -name '*.priv.pem' -o -name '.testkeys' -o -name 'id_rsa' \)` | 0 命中 |
| 2 | 卸载零残留 | 板上 装 → 卸 → 核 | `ls /etc/systemd/system \| grep -c ttbox` = 0；七个路径（五目录 + 两补漏）全不存在；`id ttbox` 失败 |
| 3 | 出货包内容 = 白名单 | 解开包逐条对清单 | 无 `docs/ tools/ platform/ modules/ tests/`、无 `*.priv.pem`、无 `.testkeys`；包内文件数 == 白名单文件数 |
| 4 | doctor 全绿 | 板端 `ttbox_doctor.sh` | 16/16 PASS，0 FAIL |
| 5 | 源码 clone 后脚本可执行 | `git ls-files -s` + 真 clone 一次 | allowlist 44 条全 `100755`；clone 到临时目录后 `test -x scripts/ttbox.sh` 为真 |
| 6 | 长稳不劣化 | 板端连续采样 ≥ 2 h（60 s 一点） | RSS 增长 < 5% 或曲线已平；fd / 线程 / 温度 / 服务 / 接口全绿 |
| 7 | 本地门禁 | 六条命令（见 §4.2） | 全 PASS |
| 8 | OTA 全链路 | 打包 → 签名 → 推板 → 安装 → 复验 | 状态 SUCCESS，且 `files_sha256` 全量复验通过 |
| 9 | 备份 → 破坏 → 恢复 | 逐项 sha256 对拍 | `DIFF=EMPTY`，属主权限保留，服务全 active |
| 10 | 版本号四处一致 | `core/CMakeLists.txt` + 发布目录名 + `RELEASE_MANIFEST.json` + 面板显示 | 四处同值 |

第 1 条和第 6 条是新增的硬门槛（此前没有）。第 9 条本轮已达成，改动后需重跑。第 10 条来自本轮新发现：面板显示 1.4.5、系统实际是 1.4.7。

---

## 二、方案总览

分四批。批 1 纯代码，可以立刻动；批 2、批 3 各卡一个业主决策；批 4 是出包与验收。

| 批次 | 内容 | 卡谁 | 阻塞性质 |
|---|---|---|---|
| 批 1 | S2 卸载脚本 · S5 git 执行位 · S3 打包脚本 · S7 版本号真源 | 无，可直接做 | 代码改动 |
| 批 2 | S4 正式 OTA 服务器地址（两处） | 业主要给地址 | 配置 |
| 批 3 | S1 清残留 + 视情况轮换密钥 + 加出厂自检 | 业主要答「出厂镜像是否以本板为模板」 | 安全 |
| 批 4 | S6 长稳判定 → 交叉编译 → 打 1.4.8 包 → 上板全量验收 | 批 1–3 完成 | 出包验收 |

**批 2 和批 3 的两个决策不落地，批 1 做得再干净也交不出去**——因为 S4 不换地址，真机「检查更新」必然失败；S1 不澄清，出厂带私钥就是授权体系失效。

本次改动涉及包内容与运维脚本，版本号建议直接出 **1.4.8**（1.4.7 保留作回滚树）。

### 执行状态回填

| 批次 | 状态 | 证据（2026-09-19） |
|---|---|---|
| 批 1 S2 | ✅ 代码完成，待板端验证 | `scripts/ttbox_uninstall.sh`：rm 改逐个循环删；DIRS 补 `/run/ttbox-mouse-passthrough`、`/lib/firmware/ttbox`；注释 9 个 unit |
| 批 1 S5 | ✅ 完成 | `git ls-files -s` 索引中 100755 恰 44 个 = allowlist 闭集；`ttbox_git_mode_check.sh` RESULT: PASS |
| 批 1 S3 | ✅ 完成（含真钥出包演练） | 新增 `deploy/pack_manifest.txt`（单一真源白名单）+ `scripts/ttbox_pack_ota.sh`（7 道断言 fail-closed）；`ttbox_fhs_init.sh` sync_tree 改读同一清单；WSL 全量签名出包 145 文件（1.4.7 为 368），断言 0–6 全过，真钥签名 + 更新器 `_verify_signature` 验签 PASS + 篡改拒绝 PASS |
| 批 1 S7 | ✅ 机制完成，版本号待批 4 出包时 bump 1.4.8 | 打包脚本以 `core/CMakeLists.txt` project VERSION 为唯一真源，目录名/manifest/包名全部派生 |
| 批 2 S4 | ⛔ 待业主给正式 OTA 地址 | 两处占位：`plugins/web/bin/ttbox-web.py:2067`、`scripts/ttbox.sh:9` |
| 批 3 S1 | ⛔ 待业主答「出厂镜像是否以本板为模板」 | 板上 `/root/ttbox-src/tools/license/.testkeys/` 配对私钥仍在 |
| 批 4 | ⏳ 排队 | 等 S6 判定（2h 采样 19:30 出结果）+ 批 2/3 |

批 1 本地门禁（2026-09-19 全绿）：check_links broken=0；conventions_gate 九项 PASS；pytest platform 53 + framework 39 + web 267 全过；OTA test_verify 0 failures；ctest 未跑（本轮未动 core，批 4 交叉编译前必跑）。

---

## 三、逐条处置方案

### S1 设备上存在授权签发私钥（最高，安全）

**现象**

| 位置 | 内容 |
|---|---|
| `/root/ttbox-src/tools/license/.testkeys/ttbox-license-2026a.priv.pem` | 119 B，与 core **内嵌**的 `ttbox-license-2026a` 公钥**配对** |
| `/root/ttbox-src/tools/ota/.testkeys/ttbox-ota-2026a.priv.pem` | OTA 2026a（现役已换 2026b，危害有限，但同样不该在设备上） |
| `/tmp/dg_cert.pem`、`/tmp/dg_key.pem` | 上一轮 OTA 联调的自签 CA 残留 |
| `/root/ttbox-src/` | 88 MB 完整源码副本，属主 `197108/197121`（Windows 侧 uid），scp 上去的调试遗留 |

**仓库侧已核实**：`git ls-files` 中不存在任何 `*.priv.pem` / `.testkeys` 文件。**这不是入库问题，是设备/镜像问题。**

**处置**

业主先回答一个问题：**出厂镜像是不是以 192.168.0.104 为模板？**

- 若「是」→ 必须清 + 轮换，两步都不能少：
  1. 清残留：`rm -rf /root/ttbox-src && rm -f /tmp/dg_cert.pem /tmp/dg_key.pem`
  2. 轮换 license 密钥对：生成 2026b 新密钥对 → 私钥入本机固定加密目录（与 ota 2026b 同规格，口令加密）→ core 重新编译内嵌新公钥 → **旧 2026a 签发的所有授权卡全部失效**，需通知渠道并准备补发
  3. 同步 `tools/license/` 的 key_id、`deploy/keys/` 的公钥、相关文档
- 若「否」→ 只需清残留（第 1 步），并在出厂镜像构建流程里加断言。

**无论走哪条都要加的护栏**：新增出厂自检，扫镜像内 `/root /tmp /opt /etc` 下的 `*.pem` / `.testkeys` / `id_rsa`，命中即 FAIL。建议挂在 `scripts/ttbox_release_verify.sh` 里，与现有出货校验同一入口。

**验证**：`find / -xdev \( -name '*.priv.pem' -o -name '.testkeys' -o -name 'id_rsa' \)` 输出为空。

---

### S2 卸载不干净（阻塞）

**根因**：`scripts/ttbox_uninstall.sh:46`

```bash
rm -f /etc/systemd/system/"${UNITS[@]}"
```

bash 里这个双引号**只作用于数组的第一个元素**，其余 8 个退化成相对路径被静默忽略。板上实测：同名命令 9 个文件名进去，只删掉 1 个，剩 8 个。

**改法（三处）**

| 行 | 现状 | 改成 |
|---|---|---|
| 46 | `rm -f /etc/systemd/system/"${UNITS[@]}"` | `for u in "${UNITS[@]}"; do rm -f "/etc/systemd/system/$u"; done` |
| 22 | `DIRS=(/opt/ttbox /etc/ttbox /var/lib/ttbox /var/log/ttbox /run/ttbox)` | 补两项：`/run/ttbox-mouse-passthrough`（板上实测存在，含 `cmd.sock` / `event.sock`）、`/lib/firmware/ttbox`（含 `hdmirx_edid.bin`） |
| 5 | 注释「7 个 unit（含 ttbox-ensure.timer 与 ttbox-ota.path/service）」 | 改为「9 个 unit」，且 `UNITS` 数组本来就是 9 条 |

顺带把 `:27` 的 plan 输出（`rm -f /etc/systemd/system/{...}` 花括号表达式）改成逐条打印，跟实际执行对齐——dry-run 的输出必须能当清单用。

**验证**（注意这脚本不可逆，验收前先 `ttbox.sh backup`）

1. 板上确认 `ls /etc/systemd/system | grep -c ttbox` 当前 = 9
2. `bash ttbox_uninstall.sh --yes` 真跑
3. 断言：`ls /etc/systemd/system | grep -c ttbox` = 0；`ls -d /run/ttbox-mouse-passthrough /lib/firmware/ttbox` 报不存在；`id ttbox` 失败
4. 用 1.4.7 包 + `ttbox_release_install.sh` 装回去，确认服务全 active

---

### S3 出货包含内部工具与文档（阻塞）

**根因（本轮新查清，比之前判断的更明确）：OTA 打包根本没有脚本。**

三套取文件的口径，互不统一：

| 环节 | 取文件规则 | 结果 |
|---|---|---|
| `scripts/ttbox_build_release.sh` | 只暂存 `bin/`（恰 1 文件）+ `lib/librknnrt.so` | 只管二进制 |
| `scripts/ttbox_fhs_init.sh` 的 `sync_tree` | **白名单闭集**：bin / lib / plugins / framework / ttbox_motion / usbproxy / scripts/edid + 指定脚本 + deploy/{keys,systemd,config} | 干净的安装树 |
| 1.4.7 的 OTA 包 | **手工 `tar` 整棵仓库树**（排除 tests / `__pycache__` / `*.pyc` / `.git`） | 368 文件，含全部内部物 |

板上 1.4.7 实测 368 个文件：

```
docs/ 123   plugins/ 81   scripts/ 42   usbproxy/ 28   platform/ 25
framework/ 19  deploy/ 17  modules/ 10  config/ 5   ttbox_motion/ 3
tools/ 8    bin/ 1   lib/ 1  + LICENSE / README.md / pyproject.toml / preview_server.py
```

对比定案：`tools/license/ttbox_license_gen.py`（发卡工具）、`tools/ota/ttbox_ota_sign.py`、`tools/ota/fake_ota_server.py`（定案 H-26 明写「不入包」）、`platform/`（定案 S1 起移出）、`docs/`（1.4 M 内部文档含探针脚本）**都不该在包里**。

因为打包是手工做的，所以「装机白名单」和「打包内容」之间没有任何机制保证一致——这就是根因。

**处置**：新增 `scripts/ttbox_pack_ota.sh` 作为**唯一打包入口**，并把白名单提成单一真源。

1. 把白名单抽成 `deploy/pack_manifest.txt`（一行一条，相对仓库根，支持目录）。
   `sync_tree` 与 `ttbox_pack_ota.sh` **都读这一份**，从此改一处两处都变。
2. `ttbox_pack_ota.sh` 的职责：
   - 按清单组装 `payload/`
   - 生成 `RELEASE_MANIFEST.json`（`files_sha256` 全量）放**包根**；发布树文件放 `payload/` 下；tar 成员**不带 `./` 前缀**（这三条是 1.4.7 联调踩出来的，见 `docs/交付前运行验证报告-2026-09-19.md:100-101`）
   - 调 `tools/ota/ttbox_ota_sign.py` 签名
3. **打包前置断言（fail-closed，任一条不过不出包）**：

| 断言 | 内容 |
|---|---|
| 禁内部物 | 包内不得出现 `docs/`、`tools/`、`platform/`、`modules/`、`tests/` |
| 禁密钥 | 包内不得出现 `*.priv.pem`、`.testkeys`、`id_rsa` |
| 禁垃圾 | 包内不得出现 `__pycache__`、`*.pyc`、`.git`、`*.bak-*`、`*.o`、`*.a`、`*.cmake` |
| `bin/` 闭集 | 恰 1 个文件，且是 `ttbox_core_main` |
| 数量自证 | 包内文件数 == 清单展开后的文件数（防漏防多） |

**白名单怎么定**（不能凭感觉删）：以 `sync_tree` 现有白名单为底，再补上板上实际被引用的路径。验证手段是板上 `grep -rn "/opt/ttbox/" /opt/ttbox/releases/1.4.7/` 找引用来源，凡是确实被运行时读取的目录必须进清单。`framework/`、`ttbox_motion/`、`usbproxy/` 已在 `sync_tree` 里，保留。

**验证**：`tar -tzf` 逐条对清单；板上装完后 `find /opt/ttbox/releases/1.4.8 -type f | wc -l` 与包内数一致；断言 `test ! -e /opt/ttbox/releases/1.4.8/{docs,tools,platform,modules}`。

---

### S4 doctor 不绿（阻塞）

**现象**：占位域名 `https://ota.ttbox.example.com` 出现在两处

| 文件:行 | 内容 |
|---|---|
| `plugins/web/bin/ttbox-web.py:2067` | `OTA_SERVER_URL = 'https://ota.ttbox.example.com'` |
| `scripts/ttbox.sh:9` | `OTA_SERVER_URL="https://ota.ttbox.example.com"` |

**处置**（二选一，都要业主定）

- 有正式地址 → 改这两处，同步 `docs/protocols/ota-server-contract.md` 里的示例
- 暂时没有 → 把默认值改成**空**，面板与 `ttbox.sh check-update` 走 fail-closed，提示「未配置更新服务器」。

后者比留一个占位域名更诚实：占位域名会让 doctor 红着，也会让真机「检查更新」必然失败却不说明原因；改成空值，doctor 能绿，用户也能一眼看懂是没配。

**验证**：`ttbox_doctor.sh` 输出 16/16 PASS、0 FAIL。

---

### S5 本机 git 索引执行位全丢（阻塞）

**现象**：索引 604 个文件**全是 100644，100755 一个没有**；allowlist 命中 44 条，全不达标。
不影响的：本工作区文件系统带执行位，打出来的包在板上是 755（实测板端 `scripts/` 全 `-rwxr-xr-x`）。
影响的：**任何人 clone 本仓库到 Linux，全部脚本不可执行**——源码交付形态不成立。

**allowlist 的判据**（来自 `scripts/ttbox_git_mode_check.sh:48-54`，5 个 glob 的并集，去重后 44 条）：

```
scripts/*.sh   scripts/*/*.sh   usbproxy/board/*.sh   usbproxy/usb-proxy   plugins/*/bin/*
```

**改法**（一条命令，44 条一起补）：

```bash
git ls-files -- "scripts/*.sh" "scripts/*/*.sh" "usbproxy/board/*.sh" \
    "usbproxy/usb-proxy" "plugins/*/bin/*" \
  | sort -u \
  | while read -r p; do git update-index --chmod=+x -- "$p"; done
```

补完提交。注意 `plugins/*/bin/*` 里有 13 个无 `.sh` 后缀的入口（`ttbox-fan`、`ttbox-web`、`ttbox-usb-proxy` 等），确认它们本来就该可执行再补。

**验证**：
`git ls-files -s | awk '$1==100755' | wc -l` = 44；
`bash scripts/ttbox_git_mode_check.sh "C:/ttbox-local"` 退出码 0；
再 `git clone` 到临时目录，`test -x scripts/ttbox.sh` 为真。

---

### S6 长稳（待判）

**30 分钟结果**：60 点 / 1800 s，脚本正常收尾。干净的五项：fd 58→58 平、线程 10→10 平、温度 65.6–66.5 ℃ 稳定、五个服务全程 active 零重启、接口全程 200。
唯一的红旗：**core RSS 34008 kB → 71292 kB，30 分钟涨 37.3 MB（2.1 倍）**，约 74 MB/h，曲线大体单调上升。

**边界**：板上 core 持续推理，RSS 随 RKNN/NPU 缓冲池、视频缓冲池变大本身可以合法增长，所以「泄漏」目前是高嫌疑、未证实。fd 与线程都平 ⇒ 如果是泄漏，是纯内存不释放型。

**2 小时观察已在板上运行**（09:19:46Z 起，60 s 一点，约 120 点，日志 `/tmp/ttbox-stability2.log`），另记 `web_rss_kb` / `prev_rss_kb` 用于定位增长源，记 `memfree_kb` / `cached_kb` 用于区分真占用与页缓存。19:19（本地）结束。

**按结果分支**

| 2 h 结果 | 判定 | 动作 |
|---|---|---|
| 后半程斜率显著小于前半程，或已趋平 | 平台期，合法缓存增长 | S6 降为 P2；交付前留这份 2 h 数据作证即可 |
| 全程斜率基本恒定 | 持续泄漏 | 定位并修，然后重跑 2 h。优先查：① RKNN 侧每帧的 `rknn_dup_context` / `rknn_outputs` / `rknn_create_mem` 是否成对释放；② V4L2 / DMA-BUF 的 mmap 是否 munmap；③ IPC 消息队列与日志缓冲；④ 每目标切换时的 tracker / PID / 曲线状态是否真的清零 |

**验证**：连续 2 h（或更长）采样，RSS 增长 < 5% 或曲线已平；fd / 线程 / 温度 / 服务 / 接口全绿。

风险量级：即便确认泄漏，按 74 MB/h 外推 1 天约 1.8 GB，当前 7.59 GB 可用内存短期不会 OOM，风险在长时间连续开机（约 4 天触及下限）。是否升级为阻塞项取决于设备预期连续运行时长。

---

### S7 版本号口径不统一（本轮新发现，出包前置）

**现象**：同一个系统上三处版本号不相同

| 位置 | 值 | 说明 |
|---|---|---|
| `core/CMakeLists.txt:14` `project(ttbox_core VERSION ...)` | **1.4.5** | 唯一的编译期真源，`version.hpp` 由它派生 |
| 板上 `/opt/ttbox/releases/` 目录名 | **1.4.7** | 打包/安装时手写的 |
| 面板 `/api/state` 的 `app_version` | **1.4.5** | `ttbox-web.py` 读 core 上报的版本 |

**影响**：用户打开面板看到「当前版本 1.4.5」，实际系统是 1.4.7，客服与售后无法对齐口径。
好消息是 OTA 的「是否已是最新」判定不看 core 版本，而是读 `/opt/ttbox/state/version`（`ttbox_ota_updater.py:94-126`，`new <= cur` 即跳过），所以不会造成重复下载；这纯粹是显示与文档口径问题。

**处置**：确立 `core/CMakeLists.txt:14` 为唯一真源，其余三处从它派生：

- 打包脚本（S3 里新增的 `ttbox_pack_ota.sh`）从 `core/CMakeLists.txt` 读版本号，写进 `RELEASE_MANIFEST.json`，并据此命名发布目录
- 面板的 `app_version` 继续读 core 上报值即可，不用改代码——只要 core 的版本号对了它就对了
- 出 1.4.8 时把 `core/CMakeLists.txt` 的版本号从 1.4.5 提到 1.4.8

**验证**：四处同值；面板 `/api/state` 的 `app_version` 与 `ls /opt/ttbox/releases/` 的目录名一致。

---

## 四、执行流程

### 4.1 按项目定死的八条规则

Windows 是源码唯一真源；本地能测的绝不上板；本地没 PASS 禁止交叉编译上板；RK3588 只跑交叉编译产物；板上只验真实硬件与真实环境；板端发现 Bug 必须回 Windows 改；每次修改都重新本地测试再重新交叉编译；没有真实证据不写 PASS。

### 4.2 本地门禁（每次改动后全跑，六条）

| 项 | 命令 | 通过标准 |
|---|---|---|
| 文档链接 | `python docs/check_links.py` | `broken_count=0` |
| 口径门禁九项 | `bash scripts/ttbox_conventions_gate.sh` | ①–⑨ 全 OK，退出码 0 |
| Core 单测 | `cd /c/ttbox-local/core && ctest --test-dir build-j --output-on-failure` | 29/29 passed |
| 平台层单测 | `python -m pytest --import-mode=importlib -q` | 92 passed |
| 面板单测 | `cd plugins/web && python -m pytest tests -q` | 267 passed |
| OTA 验签 | `python tools/ota/test_verify.py` | 10 组用例 0 失败 |

两个环境坑，写进 README 免得接手人误判：

- `ctest` 裸跑会报 29 个 `0xc0000135`（DLL 未找到），须先把 `/c/msys64/ucrt64/bin` 前置到 PATH
- `python -m pytest` 不加 `--import-mode=importlib` 收不到 `platform/tests`（stdlib 有同名 `platform` 模块）
- Core 本机构建在中文路径下要用 ASCII 符号链接 `C:/ttbox-local`，新增 target 必须重跑 `cmake -B build-j -G Ninja`

### 4.3 批次顺序

```
批 1  S2 + S5 + S3 + S7 代码改动
        ↓  本地六条门禁全绿
批 2  S4 换地址（等业主）       批 3  S1 清理 / 轮换 + 出厂自检（等业主决策）
        ↓                                    ↓
批 4  S6 判定 → WSL 交叉编译 ARM64 → 打包 1.4.8 → 上板安装 → 十条放行条件全核
```

批 1、批 2、批 3 可以并行推进（改的是不同文件），但**必须分别在本地门禁过后再合并出包**。

---

## 五、交付物清单

| # | 交付物 | 形态 |
|---|---|---|
| 1 | OTA 包 1.4.8 | 白名单合规、内容可核对、签名齐全 |
| 2 | 出厂镜像自检脚本 | 扫私钥残留，命中即 FAIL |
| 3 | 交付验收报告 | 九条放行条件逐条附证据（命令 + 原始输出） |
| 4 | 密钥轮换记录 | 仅走 S1-A 方案时需要，含新 key_id、旧卡失效范围与渠道通告 |
| 5 | 更新后的运维文档 | 卸载清单（9 unit + 7 路径）、打包流程、备份恢复 |
| 6 | `deploy/pack_manifest.txt` | 打包白名单单一真源 |

---

## 六、回滚

| 层面 | 手段 |
|---|---|
| 板端 | 1.4.7 保留作回滚树，`ttbox.sh rollback` 或 `ttbox_release_install.sh 1.4.7 --activate` |
| 仓库 | 每个批次单独提交，可单独 revert（S5 的索引模式改动也能靠 revert 恢复） |
| 打包 | 1.4.7 原包留档，出问题可退回 |
| 卸载验收 | 该脚本不可逆，验收后必须用 1.4.7 包 + `release_install` 装回并确认服务全 active |

---

## 七、风险与依赖

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| 1 | S1 走轮换 → 已发出的授权卡全部失效 | 渠道与终端用户受影响 | 轮换前先统计在用的卡，准备补发与通告 |
| 2 | S3 白名单收紧后漏掉运行时真正需要的文件 | 装上去功能缺件 | 以 `sync_tree` 现有白名单为底，用板上引用交叉验证，**不凭感觉删** |
| 3 | S5 补执行位误伤（不该可执行的被改成可执行） | 反向闭集断言会报 allowlist 外的 100755 | 44 条清单人工过一遍再提交 |
| 4 | S4 无正式地址 | 「检查更新」不可用 | 退化为空值 + fail-closed 提示，别留占位域名 |
| 5 | S6 确认泄漏且定位困难 | 出包延期 | 泄漏属 core 内存管理，与 S1–S5 无耦合，可并行排查；不阻塞包产出 |
| 6 | 业主两个决策未回 | 批 2、批 3 停滞 | 批 1 先做，把可做的做完 |
| 7 | S7 把版本号真源接到打包脚本上，若读取逻辑有误会装错目录 | 安装到错误版本目录，回滚树混乱 | 打包前置断言里加一条：目录名 == manifest 版本 == `core/CMakeLists.txt` 版本 |

---

## 八、里程碑

| 里程碑 | 内容 | 达成标志 |
|---|---|---|
| M1 | 批 1 代码改动完成 | 本地六条门禁全绿 |
| M2 | 业主回两个决策（S1 镜像模板、S4 服务器地址） | 决策落档 |
| M3 | 1.4.8 包产出 | 打包前置断言全过 + 签名成功 |
| M4 | 上板全量验收 | 十条放行条件全绿 |
| M5 | 交付物归档 | 六项交付物齐备 |

---

## 附：合规声明

本方案为交付实施方案参考，具体放行决策请结合业主验收安排确认。
