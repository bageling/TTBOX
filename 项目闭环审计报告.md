# 项目闭环审计报告

- 审计对象：TTBOX 主线与 yu 对照
- 审计目录：`C:/Users/Administrator/Desktop/TTBOX/review/TTBOX-main`
- 对照资料：`C:/Users/Administrator/Desktop/TTBOX/web-research/`、`yu_app.js`、TTBOX 内 `docs/yu-vs-ttbox-comparison.md`
- 审计时间：2026-08-30
- 结论：TTBOX 的高速 Core 主链已经成形并通过主机逻辑测试；产品级闭环仍未完成，最大缺口集中在“前端交付物、服务统一、真实 Web 验收、HID 实机、授权、更新恢复、系统控制”这几条链。

## 一、当前走到哪里

### 已形成的主线

`HDMI/V4L2 → DMA-BUF → RGA → RKNN WorkerPool → Decode/NMS → AimTargetMailbox → AimThread → HID/FIFO`

当前主线提交：`4e6b244`，分支 `main`，工作树干净，跟踪文件约 347 个。

已确认的核心能力：

- `AimThread + Pid1Controller + AlphaBetaGammaFilter + FOV + 预测 + 输出策略` 已替换旧控制链。
- `capture_buffers=12`、latest-frame 认领、三 worker 并行结构已落地。
- DFL 多输出和单输出模型解码已接入。
- `GET_CONFIG / SET_CONFIG / GET_STATUS / RUNTIME_CONTROL` 已在 IPC 中存在。
- 模型 IPC 生命周期已存在：列表、导入、校验、安装、激活、删除。
- yu → RuntimeProfile 双向转换已存在，包含热键、PID、FOV、检测阈值、裁剪、预览帧率。
- 网关已提供 yu 状态、配置、模型、HDMI、预览兼容入口。
- systemd 文件、首启、HID、EDID、性能脚本均已进入源码树。

### 已验证的性能事实

来自仓库内板端实测报告，属于历史真机证据，不是本机复测：

- Capture：约 147–150 FPS。
- Detection：约 40 FPS，受当前模型和 NPU 推理耗时限制。
- E2E：P50 约 71.5ms，P95 约 90.8ms。
- Preview：约 10.4 FPS，RGA 缩放和真实 MJPEG 流已验证。
- 当前瓶颈：模型推理和输入转换，不是 Capture。

## 二、闭环矩阵

| 能力链 | 当前状态 | 证据 | 未闭环点 |
|---|---|---|---|
| Core 逻辑 | 已验证 | 主机 Debug 构建成功，核心测试程序直接运行通过 | 仍需板端重新编译和本轮回归日志 |
| Core 高速硬件链 | 历史真机已验证 | 性能二期报告有 RK3588 实测 | 当前提交与板端部署包、板端 commit 对应关系需重新固化 |
| 参数保存 | 代码已接线 | 网关和桥接层均有 PUT 配置转换；IPC 有 SET_CONFIG 测试 | 未在当前审计轮次通过真实 Web 点击→回读→Core 状态变化复验 |
| 参数消费者 | 部分已闭环 | RuntimeProfile、worker、AimThread、FOV 有对应字段 | yu 中 KI、拉枪曲线、持续提前量等字段虽有部分 Core/测试，仍需逐项板端实际效果验收 |
| 状态回读 | 部分闭环 | GET_STATUS、GET_CONFIG、网关 `/api/state` | 网关合成数据仍有明显固定值：aim.active、输入分辨率、model_loaded、hotkey_guard 等需改为真实来源或明确显示暂未提供 |
| 预览 | 代码已接线，历史真机已验证 | `/api/preview.jpg`、`/api/preview.mjpg`，历史 SHA 连续帧证据 | 当前主线未带 `web/dist`，当前仓库无法独立启动完整前端验收 |
| 模型管理 | Core 逻辑已验证 | MODEL_* IPC 和模型注册测试通过 | 真实 `.rknn` 不进仓库；上传、校验、激活、切换、失败回滚需在板端用生产模型重新验收 |
| HID | 源码和 unit 存在 | HID 包、转发器、健康工具、服务文件存在 | 没有本轮板端 `/dev/hidg`、真实鼠标移动、hidraw→hidg 回环证据；服务链还需统一 |
| systemd | 文件存在，链路不统一 | runtime/infer/web/hid 等多个 unit | `ttbox-runtime.service`、`ttbox-infer.service`、README 描述与网关 8081、旧 `ttbox_web.py` 8080 并存，主运行入口需要定版 |
| HDMI/EDID | 板端能力和网关代码存在 | yu 对照文档记录 1920×1080@240 真机证据；网关有 v4l2 读取 | EDID API、页面、服务依赖、断信号恢复尚未完成一体化验收 |
| 授权 | 产品闭环缺失 | `DisabledAuth.cpp` 明确在线授权默认关闭；`allow_run()` 直接放行 | 正式授权接入、状态来源、失效行为、前端 license 状态均未闭环 |
| 更新/回滚 | 平台源码有骨架 | `platform/update/` 存在 | 未看到可交付升级包、版本校验、原子切换、失败回滚和断电恢复的板端证据 |
| 系统控制 | yu 对照缺失 | yu 有系统/更新/重启等页面或端点；桥接层列为 planned | TTBOX 网关未形成真实 reboot/poweroff/update/preset 生产闭环 |
| 前端交付 | 缺失 | 当前主线只有 `web/static/ttbox-bridge.js` | `web/dist`、`web/index.html` 在当前主线不存在，无法从仓库独立产出完整控制台 |

## 三、TTBOX 对 yu 还差什么

### P0：必须先补，否则不能称为完整产品

1. **统一唯一运行架构**
   - 明确生产入口到底是 `ttbox_core_main + ttbox_gateway.py:8081`，还是旧 `ttbox_web.py:8080`。
   - 清理或隔离旧 unit，统一 socket 路径、端口、安装目录、配置路径、模型目录。
   - 让 systemd 的 `After/Requires/WantedBy/ExecStart` 与真实文件一一对应。

2. **补齐前端构建和交付物**
   - 当前主线没有 `web/dist` 和前端入口文件。
   - 需要把 yu 换皮前端的源码、构建命令、桥接注入、静态资源版本策略纳入可复现交付。
   - 生成板端实际使用的 `dist`，再做干净环境启动验证。

3. **做一次真实 Web 闭环验收**
   - 页面修改置信度、IoU、FOV、裁剪、热键、PID。
   - 浏览器请求进入网关。
   - 网关写入 IPC。
   - Core RuntimeProfile 热更新。
   - worker/AimThread 读取新值。
   - GET_CONFIG 和页面回读保持一致。
   - 每项保存前后记录真实 JSON 和状态变化。

4. **HID 真机闭环**
   - 首启创建 gadget。
   - hidraw、hidg、转发器、watchdog 全部运行。
   - 热键未按下零输出，按下产生真实移动，松开立即停止。
   - 做真实鼠标接收端回环或外设验收，并保存日志。

### P1：影响 yu 功能覆盖和可交付性

5. **状态数据去固定值**
   - `/api/state` 里的 `aim.active=false`、输入宽高为 0、`model_loaded=false`、`hotkey_guard.enabled=false` 等不能长期作为合成值。
   - Core 没有数据的项目必须在页面明确显示“暂未提供”，不能伪装成已关闭、已激活或正常。

6. **模型生命周期板端验收**
   - 生产模型导入、校验、安装、激活、切换、旧模型保留、坏模型隔离、失败回滚。
   - 模型 metadata、类别名、模型输入尺寸与解码器实际行为一致。

7. **HDMI 页面和 API 完成**
   - 真输入分辨率、刷新率、像素格式、锁定状态、EDID 名称和断信号状态。
   - EDID 注入后重新读取并验证，不能只显示历史数据。

8. **预设功能**
   - 当前 `presets` 在网关状态中为空数组，桥接层也列为 planned。
   - 需要定义完整快照还是字段补丁，保存、加载、导入、恢复默认，并避免覆盖设备专属配置。

9. **授权链路**
   - 当前默认编译 `TTBOX_CORE_BUILD_AUTH=OFF`，授权状态还不是正式产品逻辑。
   - 需要独立控制面或正式授权服务、失效策略、离线缓存策略、前端真实状态和测试。

10. **升级、回滚、恢复**
    - 生成真实升级包和 manifest。
    - 校验版本、哈希和架构。
    - 原子安装、失败回滚、断电恢复、配置保留、模型保留。
    - 冷启动后服务、Web、HID、模型和配置全部恢复。

## 四、当前发现的直接问题

1. **本机 CTest 命令没有正确运行**：CMake 使用多配置生成器，直接 `ctest --test-dir core/build_audit` 找不到 Debug 可执行文件，报告为 11 项 Not Run；随后已直接运行 11 个 Debug 测试程序，核心总测试显示 `101 tests passed, 0 tests failed`，其余专项测试全部 PASS。下次应使用 `ctest -C Debug` 或固定单配置生成器。
2. **当前主线没有前端产物**：`web/dist`、`web/index.html` 均缺失；这直接阻断“从当前仓库安装并打开 Web 控制台”。
3. **systemd 文件不完整或不一致**：目录中没有 `ttbox-core.service`，但 `ttbox-infer.service` 写着 `After=ttbox-core.service`；实际存在的是 `ttbox-runtime.service`。这是生产启动链的硬断点。
4. **网关和旧 Web 服务并存**：`scripts/ttbox_gateway.py` 默认 8081，而 `core/tools/web/ttbox-web.service` 启动旧 `ttbox_web.py` 并设置 8080，需要定版，避免两套控制面同时存在。
5. **网关有合成状态字段**：代码注释说“不伪造”，但 `/api/state` 中仍有固定状态和空结构，真实产品验收时会造成误判。
6. **配置默认值与板端生产值存在边界**：仓库 `config/default.json` 仍是基础模板，历史板端性能报告使用过 `capture_buffers=12`、预览 10 等生产配置；需要把最终生产配置、备份、版本和部署来源绑定起来。
7. **授权默认关闭**：这适合当前 Core 测试，但不等于正式 yu 产品授权闭环完成。

## 五、最终判断

- **Core 算法与主机逻辑：已基本完成。**
- **RK3588 高速链路：历史真机已达到可用性能，但当前版本需要重新做板端一致性验收。**
- **yu 换皮和参数翻译：主干已经接通，真实页面到 Core 的逐项验收还没有闭环。**
- **完整产品交付：尚未完成。**
- **当前最大断点不是算法，而是交付架构：前端产物缺失、服务入口分裂、HID/模型/HDMI/授权/升级缺少一套最终板端验收证据。**

## 六、最短收口顺序

1. 先统一生产入口、端口、socket、配置和 systemd。
2. 补齐可复现前端源码与 `dist` 构建产物。
3. 做 Web 参数保存/回读/实时生效全量验收。
4. 做模型生命周期和预览真实验收。
5. 做 HID gadget、转发、热键、真实输出验收。
6. 做 HDMI/EDID、冷启动、服务故障恢复验收。
7. 最后封装授权、升级、回滚和正式交付包。

本报告只记录本轮实际读取、执行和历史证据；板端当前状态、正式包生成和生产 Web 点击验收标记为待补证据。