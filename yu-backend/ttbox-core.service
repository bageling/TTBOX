[Unit]
Description=TTBOX Core (C++)
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/ttbox
ExecStart=/opt/ttbox/core/build/ttbox_core_main --config /opt/ttbox/config/default.json --ipc /tmp/ttbox_core.sock
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
