[Unit]
Description=TTBox performance mode (lock freq + dma latency + irq affinity)
After=multi-user.target
Before=ttbox-runtime.service

[Service]
Type=simple
ExecStart=/opt/ttbox/scripts/ttbox-perf.sh
LimitMEMLOCK=infinity
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
